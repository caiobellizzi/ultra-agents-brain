---
phase: 13-knowledge-surface-activation
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - .planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md
autonomous: false
requirements: [KNOW-01, KNOW-02, KNOW-03, OBS-01]
user_setup:
  - Operator runs the deploy + reindex commands listed in tasks 1-3 against the VPS (uab-brain.service). Cannot be automated by the executor — the deploy machine is the operator's workstation, not the planning context.
  - Operator must have SSH access to `root@uab-brain` (or whatever the VPS user/host is) and `psql` access to both `agno_sessions` and `agno_knowledge` databases on the VPS.
  - Operator must have a browser session signed in to https://os.agno.com pointing at the deployed AgentOS instance.

must_haves:
  decisions:
    - "Verification gate fires only after 13-01 + 13-02 commits are on `main` AND the VPS has been redeployed with both changes."
    - "All 7 verification steps from CONTEXT.md §verification_protocol must pass. Any failure → open a fix-up plan; do not declare phase 13 done."
    - "Evidence captured in VERIFICATION.md uses the same shape phase 11 + 12 used: copy-paste of CLI output, psql query results, screenshots referenced by relative path."
    - "Phase status flips to `Complete` in ROADMAP.md only when VERIFICATION.md has `status: passed` and the 4 ROADMAP success criteria are individually checked off."
  truths:
    - VPS deploy of `main` HEAD includes commits from 13-01 (`feat(13-01): knowledge write path …`) and 13-02 (`feat(13-02): InstrumentedKnowledge wrapper …`).
    - `python -m agentos.knowledge --reindex` run on the VPS as the `uabrain` user exits 0 and reports `Indexed N files (M skipped, 0 errors)` where N matches the vault `.md` count.
    - `psql -d agno_knowledge -c "SELECT count(*) FROM ai.vault"` returns a count > 0 (PgVector chunking multiplies by ~1-4× the file count depending on file size).
    - `psql -d agno_sessions -c "SELECT count(*) FROM ai.agno_knowledge WHERE linked_to='ultra-brain-vault'"` returns N (one row per indexed file).
    - `curl -fsS http://127.0.0.1:8000/knowledge/config` (or the equivalent through the AgentOS public endpoint) returns JSON whose `available_ids` contains a non-empty entry derived from `("ultra-brain-main", "agno_knowledge", "ultra-brain-vault")`.
    - `curl -fsS "http://127.0.0.1:8000/knowledge/content?db_id=ultra-brain-main"` returns HTTP 200 with non-empty `data[]` array.
    - A second reindex run on the unchanged vault reports `Indexed 0 files (N skipped, 0 errors)` and `count(*)` on `ai.vault` is unchanged before/after.
    - A Telegram chat to the bot triggers a RAG hit; the matched content row's `access_count` increases by ≥1 in `ai.agno_knowledge`.
    - `journalctl -u uab-brain.service` shows at least one `"op":"index"` line (from reindex) and at least one `"op":"search"` line (from the chat RAG run) on the `agentos.knowledge` logger.
    - os.agno.com Knowledge tab renders the `ultra-brain-vault` instance with non-zero content count and the bumped `access_count` on the matched file.
  artifacts:
    - .planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md (status: passed)
  key_links:
    - CONTEXT.md §verification_protocol (the 7-step gate this plan executes)
    - Phase 11 + 12 VERIFICATION.md (templates to mirror)
---

<objective>
Execute the phase-end verification gate for phase 13. This plan does no code changes — it runs the 7 verification steps from CONTEXT.md §verification_protocol on the deployed VPS, captures evidence, and writes VERIFICATION.md. The plan also flips Phase 13 status to `Complete` in ROADMAP.md if and only if all gates pass.

If any gate fails: do NOT mark complete; instead, open a fix-up plan (`/gsd:plan-phase 13 --gaps` or a new sub-plan) and treat the failure as a blocker.

This plan is operator-driven by design — the executor cannot SSH to the VPS or sign in to os.agno.com on the operator's behalf. The tasks below are structured so the operator can run them in order and paste the outputs into VERIFICATION.md as it goes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/13-knowledge-surface-activation/13-CONTEXT.md
@.planning/phases/13-knowledge-surface-activation/13-RESEARCH.md
@.planning/phases/13-knowledge-surface-activation/13-VALIDATION.md
@.planning/phases/13-knowledge-surface-activation/13-01-PLAN.md
@.planning/phases/13-knowledge-surface-activation/13-02-PLAN.md
@.planning/phases/11-memory-surface-activation/11-VERIFICATION.md
@.planning/phases/12-evals-surface-activation/12-VERIFICATION.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Deploy phase 13 code to the VPS</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/11-memory-surface-activation/SUMMARY.md (deploy procedure — likely same)
    - .planning/phases/12-evals-surface-activation/SUMMARY.md (deploy procedure)
    - `ops/` and `deploy/` directories in the repo for any scripted deploy
  </read_first>
  <action>
    On the operator workstation:

    1. Confirm `main` HEAD includes the 13-01 + 13-02 commits:
       ```
       git log --oneline | head -10 | grep -E '13-0[12]'
       ```
       Expect two lines matching `feat(13-01): …` and `feat(13-02): …`.

    2. Sync code to the VPS (whichever pattern phases 11/12 used — `rsync`, `git pull`, or a deploy script under `ops/`):
       ```
       # Example shape (adapt to the project's actual deploy command):
       ssh root@uab-brain "cd /srv/ultra-agents-brain && git pull --ff-only"
       ssh root@uab-brain "systemctl restart uab-brain.service"
       ```

    3. Confirm the service is running:
       ```
       ssh root@uab-brain "systemctl status uab-brain.service --no-pager | head -20"
       ```
       Expect `Active: active (running)`.

    4. Confirm the new code is in place on the VPS:
       ```
       ssh root@uab-brain "cd /srv/ultra-agents-brain && grep -c 'def reindex' agentos/knowledge.py && ls agentos/instrumented_knowledge.py"
       ```
       Expect `1` and the file listed.

    Capture the output of each command and paste into VERIFICATION.md §"Deploy evidence" in task 6.
  </action>
  <verify>
    <automated>
      # No automated verify — this is an operator step. The acceptance criteria
      # are evidence captured in VERIFICATION.md.
    </automated>
    <human-check>
      Operator confirms: (a) systemd service is active, (b) `agentos/instrumented_knowledge.py` exists on the VPS, (c) `agentos/knowledge.py` exports `reindex`.
    </human-check>
  </verify>
  <acceptance_criteria>
    - VPS systemd service `uab-brain.service` is `Active: active (running)` after restart.
    - `agentos/instrumented_knowledge.py` exists on the VPS filesystem.
    - `agentos/knowledge.py` on the VPS contains `def reindex(`.
    - Output from each step captured for VERIFICATION.md.
  </acceptance_criteria>
  <done>
    VPS is running phase-13 code. Ready for first reindex.
  </done>
</task>

<task type="execute">
  <name>Task 2: First reindex on VPS (KNOW-01 + DIAG-BL-05 + OBS-01 write)</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/13-knowledge-surface-activation/13-CONTEXT.md §verification_protocol steps 1-3
  </read_first>
  <action>
    On the VPS (or via SSH):

    1. Confirm vault is mounted and file count:
       ```
       ssh root@uab-brain "find /srv/second-brain -name '*.md' | wc -l"
       ```
       Record the count as N.

    2. Run reindex AS THE SERVICE USER (avoids file-permission mismatches — landmine in CONTEXT.md threat model):
       ```
       ssh root@uab-brain "sudo -u uabrain bash -c 'cd /srv/ultra-agents-brain && source .venv/bin/activate && python -m agentos.knowledge --reindex' 2>&1 | tee /tmp/reindex-13.log"
       ```
       Expect: per-file `[indexed]` lines, then a summary `Indexed N files (0 skipped, 0 errors) in Xs`, exit code 0.

    3. Row counts in Postgres:
       ```
       ssh root@uab-brain 'psql -d agno_knowledge -c "SELECT count(*) FROM ai.vault"'
       ssh root@uab-brain 'psql -d agno_sessions -c "SELECT count(*) FROM ai.agno_knowledge WHERE linked_to='\''ultra-brain-vault'\''"'
       ```
       Expect: first query returns ≥ N (chunk multiplier), second returns exactly N.

    4. API check via the loopback endpoint:
       ```
       ssh root@uab-brain "curl -sS http://127.0.0.1:8000/knowledge/config" | jq '.available_ids'
       ssh root@uab-brain "curl -sS 'http://127.0.0.1:8000/knowledge/content?db_id=ultra-brain-main'" | jq '.data | length'
       ```
       Expect: first returns a JSON array containing a UUID-shaped string (NOT `[]`); second returns a positive integer.

    5. OBS-01 write log present in journald:
       ```
       ssh root@uab-brain 'journalctl -u uab-brain.service --since "5 minutes ago" | grep -c "\"op\":\"index\""'
       ```
       Wait — `journalctl` shows the service's logs, but the reindex CLI runs as `uabrain` outside the systemd unit. Use this instead:
       ```
       ssh root@uab-brain 'cat /tmp/reindex-13.log | grep -c "OBS-01 knowledge write:"'
       ```
       Expect: a number ≥ N (one OBS-01 line per indexed file).

    Capture all outputs into VERIFICATION.md §"Reindex evidence".
  </action>
  <verify>
    <automated>
      # Operator-run; evidence captured in VERIFICATION.md.
    </automated>
    <human-check>
      Operator confirms all four checks (1-4 above) pass with the expected values.
    </human-check>
  </verify>
  <acceptance_criteria>
    - CLI summary on VPS reports `Indexed N files (0 skipped, 0 errors)` with N matching `find` count.
    - `SELECT count(*) FROM ai.vault` ≥ N.
    - `SELECT count(*) FROM ai.agno_knowledge WHERE linked_to='ultra-brain-vault'` = N.
    - `/knowledge/config` returns a non-empty `available_ids` array.
    - `/knowledge/content?db_id=ultra-brain-main` returns HTTP 200 with `data[]` of length > 0.
    - `/tmp/reindex-13.log` contains ≥ N `OBS-01 knowledge write` lines with `"op":"index"`.
  </acceptance_criteria>
  <done>
    KNOW-01, DIAG-BL-05, DIAG-BL-06, OBS-01 (write) verified on the deployed VPS.
  </done>
</task>

<task type="execute">
  <name>Task 3: Second reindex — idempotency check (KNOW-03)</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/13-knowledge-surface-activation/13-CONTEXT.md §verification_protocol step 4
  </read_first>
  <action>
    Without changing the vault contents, re-run reindex:

    1. Record `ai.vault` row count before:
       ```
       BEFORE=$(ssh root@uab-brain "psql -d agno_knowledge -tAc 'SELECT count(*) FROM ai.vault'")
       echo "BEFORE=$BEFORE"
       ```

    2. Second reindex:
       ```
       ssh root@uab-brain "sudo -u uabrain bash -c 'cd /srv/ultra-agents-brain && source .venv/bin/activate && python -m agentos.knowledge --reindex' 2>&1 | tee /tmp/reindex-13b.log"
       ```
       Expect: summary `Indexed 0 files (N skipped, 0 errors) in Xs`. Exit 0.

    3. Record `ai.vault` row count after:
       ```
       AFTER=$(ssh root@uab-brain "psql -d agno_knowledge -tAc 'SELECT count(*) FROM ai.vault'")
       echo "AFTER=$AFTER"
       ```
       Assert `BEFORE == AFTER`.

    Capture the two counts + the summary line into VERIFICATION.md §"Idempotency evidence".
  </action>
  <verify>
    <automated>
      # Operator-run.
    </automated>
    <human-check>
      Operator confirms `BEFORE == AFTER` and the second reindex reported `Indexed 0 files`.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Second reindex summary contains `Indexed 0 files` and a non-zero `skipped` count.
    - `BEFORE == AFTER` for `ai.vault` row count.
    - Exit code 0.
  </acceptance_criteria>
  <done>
    KNOW-03 verified — reindex is row-count idempotent.
  </done>
</task>

<task type="execute">
  <name>Task 4: Chat agent RAG-hit triggers access_count bump (KNOW-02 + OBS-01 access)</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/13-knowledge-surface-activation/13-CONTEXT.md §verification_protocol step 5
  </read_first>
  <action>
    1. Pick a vault file with recognizable content (something the operator KNOWS is in the vault, e.g., a recent daily note or a specific topic note). Record its filename (e.g., `daily/2026-05-20.md`).

    2. Record `access_count` before:
       ```
       BEFORE=$(ssh root@uab-brain "psql -d agno_sessions -tAc \"SELECT access_count FROM ai.agno_knowledge WHERE name='<FILE>' AND linked_to='ultra-brain-vault'\"")
       echo "BEFORE=$BEFORE"
       ```

    3. Send a Telegram message to the bot whose content is a question that file can answer. (Or, alternatively, hit the chat endpoint directly via curl — whichever path the operator uses for daily verification.)

    4. Wait ~5 seconds for the agent to respond. Confirm the response references the vault content.

    5. Record `access_count` after:
       ```
       AFTER=$(ssh root@uab-brain "psql -d agno_sessions -tAc \"SELECT access_count FROM ai.agno_knowledge WHERE name='<FILE>' AND linked_to='ultra-brain-vault'\"")
       echo "AFTER=$AFTER"
       ```
       Assert `AFTER >= BEFORE + 1`.

    6. OBS-01 access log present in journald (this one IS inside the systemd service):
       ```
       ssh root@uab-brain 'journalctl -u uab-brain.service --since "1 minute ago" | grep "OBS-01 knowledge search"'
       ```
       Expect at least one matching line whose JSON body has `"op":"search"`, `"hit_count" >= 1`, `"status":"ok"`.

    7. UI check — operator opens https://os.agno.com (logged in), navigates to the Knowledge tab, confirms:
       - The `ultra-brain-vault` knowledge instance is listed.
       - Its content count matches N (from task 2).
       - The matched file (e.g., `daily/2026-05-20.md`) shows the new `access_count` value.

    8. Take a screenshot of the Knowledge tab; save under `.planning/phases/13-knowledge-surface-activation/screenshots/knowledge-tab-access-count.png`. Reference it in VERIFICATION.md.

    Capture: BEFORE, AFTER, the journald log line, and the screenshot reference.
  </action>
  <verify>
    <automated>
      # Operator-run; UI step requires a browser.
    </automated>
    <human-check>
      Operator confirms: agent reply cited the vault content; `access_count` bumped; journald shows the OBS-01 search line; UI renders the bumped count.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `AFTER >= BEFORE + 1` for the queried file's `access_count`.
    - `journalctl -u uab-brain.service` shows ≥1 `"OBS-01 knowledge search"` line with `"hit_count" >= 1` since the chat send.
    - os.agno.com Knowledge tab screenshot shows `ultra-brain-vault` instance + the bumped `access_count` on the matched file.
    - Screenshot file exists at `.planning/phases/13-knowledge-surface-activation/screenshots/knowledge-tab-access-count.png`.
  </acceptance_criteria>
  <done>
    KNOW-02 verified end-to-end. UI surface confirmed working.
  </done>
</task>

<task type="execute">
  <name>Task 5: Stub fallback check (DIAG-BL-06)</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/13-knowledge-surface-activation/13-CONTEXT.md §threat_model "PgVector migration not applied" + §decisions D-04
  </read_first>
  <action>
    Verify the WARNING log emits when `POSTGRES_DSN_KNOWLEDGE` is unset. This cannot be tested on prod without breaking it — so do it locally:

    1. On the operator workstation:
       ```
       cd /Users/caiobellizzi/Documents/Projects/ultra-agents-brain
       unset POSTGRES_DSN_KNOWLEDGE
       .venv/bin/python -c "
       import logging, sys
       logging.basicConfig(level=logging.WARNING, stream=sys.stdout)
       from agentos.knowledge import make_knowledge
       kb = make_knowledge()
       print('--', type(kb).__name__, kb.name, kb.vector_db, kb.contents_db)
       "
       ```
    2. Expect output containing a line like:
       ```
       WARNING agentos.knowledge OBS-01 knowledge stub: {"path":"knowledge","status":"stub-fallback","reason":"...","db_id":null}
       ```
       And the final line: `-- Knowledge ultra-brain-vault None None` (or InstrumentedKnowledge if 13-02 wired that path differently — verify against actual implementation).

    Capture the output into VERIFICATION.md §"Stub-fallback evidence".

    Reset env after:
    ```
    export POSTGRES_DSN_KNOWLEDGE=<original value>
    ```
  </action>
  <verify>
    <automated>
      # Operator-run locally.
    </automated>
    <human-check>
      WARNING line present in output; final type printed is the expected Knowledge variant.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Local run with `POSTGRES_DSN_KNOWLEDGE` unset emits a WARNING-level line on `agentos.knowledge` whose message body parses as JSON with `"status":"stub-fallback"` and `"db_id":null`.
    - Process exits 0 (the stub is import-safe).
  </acceptance_criteria>
  <done>
    DIAG-BL-06 verified — no more silent stub.
  </done>
</task>

<task type="execute">
  <name>Task 6: Write VERIFICATION.md and update ROADMAP status</name>
  <files>
    .planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/11-memory-surface-activation/11-VERIFICATION.md (template)
    - .planning/phases/12-evals-surface-activation/12-VERIFICATION.md (template)
    - All outputs captured in tasks 1-5
  </read_first>
  <action>
    1. Create `.planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md` with frontmatter:
       ```yaml
       ---
       phase: 13
       slug: knowledge-surface-activation
       status: passed
       verified_at: <ISO date>
       verified_by: <operator name>
       ---
       ```

    2. Below the frontmatter, include sections (one per gate from CONTEXT.md §verification_protocol):
       - `## Reindex sanity (KNOW-01)` — paste task 2 CLI summary
       - `## Row count (KNOW-01)` — paste two psql counts
       - `## API surface (KNOW-01)` — paste `/knowledge/config` + `/knowledge/content` outputs
       - `## Idempotency (KNOW-03)` — paste task 3 BEFORE/AFTER + summary
       - `## RAG-hit (KNOW-02)` — paste BEFORE/AFTER access_count + screenshot reference
       - `## OBS-01 logs` — paste a sample `op:"index"` line and a sample `op:"search"` line
       - `## Stub fallback (DIAG-BL-06)` — paste task 5 local output
       - `## UI verification` — screenshot reference + one-paragraph confirmation

    3. End with a "Sign-off" block:
       ```
       ## Sign-off

       All 4 ROADMAP success criteria verified:
       - [x] 1. Vault `.md` content appears in AgentOS Knowledge tab; row counts in pgvector match expected doc count.
       - [x] 2. Agentic RAG query during agent run records a knowledge-access event visible in UI.
       - [x] 3. Reindex run twice → no duplicate rows.
       - [x] 4. Knowledge write/access paths emit structured log lines.

       Phase 13 status: COMPLETE.
       ```

    4. Edit `.planning/ROADMAP.md`: locate the Phase 13 line and change its status marker from `Pending` to `Complete` (mirror the convention used for Phase 11 + 12 closeouts — likely a status badge or `**Status:** Complete` line in the phase section).

    5. Commit:
       ```
       git add .planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md \
               .planning/phases/13-knowledge-surface-activation/screenshots/ \
               .planning/ROADMAP.md
       git commit -m "docs(13): phase 13 verification PASSED — knowledge surface activated"
       ```
  </action>
  <verify>
    <automated>
      rtk git log -1 --pretty=%s
    </automated>
    <human-check>
      VERIFICATION.md contains evidence for all 4 ROADMAP criteria and frontmatter `status: passed`. ROADMAP.md shows Phase 13 as Complete.
    </human-check>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/13-knowledge-surface-activation/13-VERIFICATION.md` exists with frontmatter `status: passed`.
    - File contains 8 sections (one per task 1-5 + UI) with pasted evidence.
    - All 4 ROADMAP success criteria show `[x]` in the sign-off block.
    - `.planning/ROADMAP.md` shows Phase 13 as Complete (whatever exact marker phase 11/12 used).
    - `git log -1 --pretty=%s` returns `docs(13): phase 13 verification PASSED — knowledge surface activated`.
  </acceptance_criteria>
  <done>
    Phase 13 is closed. Knowledge surface is live on the VPS, observable in the UI, and locked behind documented evidence.
  </done>
</task>

</tasks>

<threat_model>

| Threat | Mitigation | Tested by |
|---|---|---|
| T-13-03-A: Reindex run as wrong OS user → permission errors | Task 2 explicitly runs as `uabrain` via `sudo -u` | Task 2 step 2 |
| T-13-03-B: Verification "passes" with stale code on the VPS | Task 1 verifies `reindex` symbol exists on the VPS after deploy | Task 1 step 4 |
| T-13-03-C: PgVector chunking inflates row count → check fails | RESEARCH R-02 + verification step 2 — `ai.vault` count is ≥ N (allow chunk multiplier); `ai.agno_knowledge` count is == N | Task 2 step 3 |
| T-13-03-D: UI shows old cached state → false negative | Operator instructed to reload the tab after the chat send before checking access_count | Task 4 step 7 |
| T-13-03-E: Bumped `access_count` reverts on next reindex (overwrite via upsert) | RESEARCH R-04: reindex's `upsert=True` on PgVector wraps the embedding row, not the `ai.agno_knowledge` row's `access_count`. Confirm with task 3 (BEFORE==AFTER on row count, but ALSO verify `access_count` not reset by adding a step to task 3 if needed) | Task 4 + manual cross-check |
| T-13-03-F: Operator forgets a step, marks phase Complete with missing evidence | Task 6 sign-off block requires all 4 ROADMAP criteria checked; reviewer should verify on commit | Manual code review of VERIFICATION.md before merge |

</threat_model>

<verification>
This plan IS the verification gate. Plan-level "verification" is the operator confirming VERIFICATION.md contains all required evidence and ROADMAP.md reflects the Complete status.

After this plan completes:
- All 4 ROADMAP success criteria for Phase 13 are individually verified with copy-paste evidence in VERIFICATION.md.
- Phase 13 is `Complete` in ROADMAP.md.
- The knowledge surface is live on os.agno.com — operators can use it as a daily PKM aid.

Phase 13 closes. Phase 14 (Approvals) can start.
</verification>
