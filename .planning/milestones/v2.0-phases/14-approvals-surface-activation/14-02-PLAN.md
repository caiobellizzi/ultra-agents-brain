---
phase: 14-approvals-surface-activation
plan: "02"
type: tdd
wave: 2
depends_on:
  - 14-01
files_modified:
  - channels/telegram_adapter.py
  - tests/test_telegram_adapter.py
autonomous: true
requirements:
  - APPR-02

must_haves:
  truths:
    - "Telegram Approve callback calls POST /approvals/{approval_id}/resolve with status='approved' BEFORE calling /runs/{run_id}/continue"
    - "Telegram Deny callback calls POST /approvals/{approval_id}/resolve with status='rejected' BEFORE calling /runs/{run_id}/continue"
    - "Resolve failure sends a Telegram error message and releases the _RESOLVED_RUNS guard for retry"
    - "409 on resolve is treated as idempotent success (row already resolved)"
    - "tool_call_id is used to match the correct approval row when multiple pending requirements exist"
    - "Existing duplicate-tap guard (_RESOLVED_RUNS) is preserved"
    - "4 new TestApprovalBridge tests pass alongside all existing test_telegram_adapter.py tests"
  artifacts:
    - path: "channels/telegram_adapter.py"
      provides: "_resolve_approval_row async helper and handle_callback wiring"
      contains: "async def _resolve_approval_row"
    - path: "tests/test_telegram_adapter.py"
      provides: "TestApprovalBridge class with 4 tests for APPR-02 bridge behavior"
      contains: "class TestApprovalBridge"
  key_links:
    - from: "channels/telegram_adapter.py::handle_callback"
      to: "_resolve_approval_row"
      via: "await _resolve_approval_row(client, run_id, tool_call_id, confirmed, tg_user_id) call before /continue"
      pattern: "_resolve_approval_row"
    - from: "_resolve_approval_row"
      to: "POST /approvals/{approval_id}/resolve"
      via: "httpx.AsyncClient GET then POST"
      pattern: "AGENTOS_BASE_URL.*approvals"
---

<objective>
Add _resolve_approval_row async helper to channels/telegram_adapter.py and wire it into handle_callback so every Approve/Deny tap first resolves the native ai.agno_approvals row before calling /continue. This closes the APPR-02 gap: previously, /continue ran but left the approval row in status='pending'.

Purpose: After this plan, Telegram approve/deny produces the full state transition — row flips pending→approved/rejected AND the run continues — satisfying D-05, D-06, D-07, D-08.

Output: _resolve_approval_row helper in telegram_adapter.py, TestApprovalBridge class in test_telegram_adapter.py (4 tests), all existing adapter tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-approvals-surface-activation/14-CONTEXT.md
@.planning/phases/14-approvals-surface-activation/14-RESEARCH.md
@.planning/phases/14-approvals-surface-activation/14-PATTERNS.md
@.planning/phases/14-approvals-surface-activation/14-01-SUMMARY.md
</context>

<interfaces>
<!-- Exact interfaces extracted from channels/telegram_adapter.py and Agno router source -->

From channels/telegram_adapter.py (existing):
- _PAUSED_TOOLS: dict[str, list[dict]] — keyed by run_id, value is list of cached tool execution dicts
- _RESOLVED_RUNS: set[str] — run_ids already processed; duplicate-tap guard
- AGENTOS_BASE_URL: str — env var read at module top
- handle_callback(client: httpx.AsyncClient, query: dict) -> None — main callback handler
- send_approval_buttons sets _PAUSED_TOOLS[run_id] with tool dicts containing 'tool_call_id', 'tool_name', 'tool_args', 'run_id'
- Current handle_callback flow (relevant portion):
    1. validate callback_data shape (4-part: action|agent_id|run_id|tool_call_id)
    2. if run_id in _RESOLVED_RUNS: answer_callback + return  (duplicate guard)
    3. _RESOLVED_RUNS.add(run_id)
    4. confirmed = (action == "approve")
    5. cached = _PAUSED_TOOLS.pop(run_id, None)
    6. POST /agents/{agent_id}/runs/{run_id}/continue ...
    7. on non-409 error: _RESOLVED_RUNS.discard(run_id)

From .venv/lib/python3.13/site-packages/agno/os/routers/approvals/router.py:
- GET /approvals?run_id=<id>&status=pending&approval_type=required → returns {"data": [...]}
- Each row has: "id" (approval_id), "tool_execution": {"tool_call_id": ...}, "status", "run_status"
- POST /approvals/{approval_id}/resolve body: {"status": "approved"|"rejected", "resolved_by": "<str>", "resolution_data": {}}
- 409 means already resolved (idempotent case — treat as success)

New function signature to add to channels/telegram_adapter.py (per D-06 pattern from RESEARCH.md):
  async def _resolve_approval_row(
      client: httpx.AsyncClient,
      run_id: str,
      tool_call_id: str,
      confirmed: bool,
      tg_user_id: int,
  ) -> bool:
      Returns True on success (resolved or 409), False on failure.
      Failures log at WARNING/ERROR level but do NOT raise.

Integration point in handle_callback:
  - Call _resolve_approval_row AFTER _RESOLVED_RUNS.add(run_id) and BEFORE the /continue POST
  - If _resolve_approval_row returns False: send Telegram error message, _RESOLVED_RUNS.discard(run_id), return (D-16: surface error, allow retry)
  - On success: proceed to existing /continue call unchanged
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing TestApprovalBridge tests in test_telegram_adapter.py</name>
  <files>tests/test_telegram_adapter.py</files>
  <read_first>
    - tests/test_telegram_adapter.py (full file — TestApproveDoubleTap class as the exact pattern to mirror for new tests)
    - channels/telegram_adapter.py (handle_callback lines ~421-530 — exact flow to test)
    - .venv/lib/python3.13/site-packages/agno/os/routers/approvals/router.py (response shape for GET /approvals and POST /approvals/{id}/resolve)
  </read_first>
  <behavior>
    Add class TestApprovalBridge(unittest.TestCase) after TestApproveDoubleTap. All 4 tests must FAIL until _resolve_approval_row is added to telegram_adapter.py:

    test_approve_resolves_then_continues (VALIDATION.md: 14-02-01):
    - Set up mock AsyncClient with responses:
      GET /approvals?run_id=... → 200 {"data": [{"id": "ap-001", "tool_execution": {"tool_call_id": "tc-123"}, "status": "pending"}]}
      POST /approvals/ap-001/resolve → 200 {}
      POST /runs/.../continue → 200 {"run_status": "completed", "content": []}
    - Populate _PAUSED_TOOLS[run_id] with a tool dict (tool_call_id='tc-123')
    - Invoke handle_callback with approve callback_data
    - Assert POST /approvals/ap-001/resolve was called with status='approved'
    - Assert /continue was called AFTER /approvals/ap-001/resolve (call order matters)

    test_deny_resolves_rejected (VALIDATION.md: 14-02-02):
    - Same setup but deny action
    - Assert POST /approvals/ap-001/resolve called with status='rejected'
    - Assert /continue was still called (run must not stay paused on deny)

    test_resolve_failure_releases_guard (VALIDATION.md: 14-02-03):
    - Mock GET /approvals → 200 with rows, but POST /approvals/ap-001/resolve → 500
    - Invoke handle_callback with approve callback_data
    - Assert /continue was NOT called
    - Assert run_id was removed from _RESOLVED_RUNS (guard released for retry)
    - Assert send_message (Telegram error) was called

    test_resolve_409_is_ok (VALIDATION.md: 14-02-04):
    - Mock POST /approvals/ap-001/resolve → 409
    - Assert handle_callback does NOT treat this as failure
    - Assert /continue IS called (409 = idempotent success)

    Run: pytest tests/test_telegram_adapter.py::TestApprovalBridge -x — all 4 must FAIL (AttributeError: module has no _resolve_approval_row or call order assertion fails).
  </behavior>
  <action>
    Append class TestApprovalBridge to tests/test_telegram_adapter.py.

    Use the same _import_adapter_with_env() pattern and asyncio.run() execution style as TestApproveDoubleTap. The mock client should use AsyncMock with side_effect lists for sequential calls or use a custom async mock that inspects URL and method to route responses.

    setUpClass should call _import_adapter_with_env() and store as cls.mod. Clear _PAUSED_TOOLS and _RESOLVED_RUNS in setUp() to ensure test isolation.

    Do not add fenced code blocks. Describe behavior expectations only.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-agents-brain && python -m pytest tests/test_telegram_adapter.py::TestApprovalBridge -x 2>&1 | grep -E "FAILED|ERROR|AttributeError" | head -5</automated>
  </verify>
  <done>TestApprovalBridge class added to test_telegram_adapter.py; 4 tests fail with AttributeError or assertion error (not ImportError)</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Add _resolve_approval_row to telegram_adapter.py and wire into handle_callback</name>
  <files>channels/telegram_adapter.py</files>
  <read_first>
    - channels/telegram_adapter.py (full file — handle_callback, send_approval_buttons, _PAUSED_TOOLS, _RESOLVED_RUNS, existing /continue call at ~lines 478-530)
    - tests/test_telegram_adapter.py (TestApprovalBridge — test expectations to satisfy)
    - .planning/phases/14-approvals-surface-activation/14-PATTERNS.md (_resolve_approval_row helper pattern from RESEARCH.md section)
  </read_first>
  <action>
    Add async def _resolve_approval_row(client, run_id, tool_call_id, confirmed, tg_user_id) -> bool to channels/telegram_adapter.py.

    Implementation (per D-06):
    1. Build status_value = "approved" if confirmed else "rejected"
    2. resolved_by = f"telegram:{tg_user_id}"
    3. GET {AGENTOS_BASE_URL}/approvals with params run_id, status="pending", approval_type="required"
    4. On non-200 response or network error: log.warning, return False
    5. Parse rows = response.json().get("data") or []
    6. Find approval_id: iterate rows, match row where row.get("tool_execution", {}).get("tool_call_id") == tool_call_id; fall back to first row if none match (when tool_call_id absent in row)
    7. If no approval_id found: log.warning, return False
    8. POST {AGENTOS_BASE_URL}/approvals/{approval_id}/resolve with JSON body {status: status_value, resolved_by: resolved_by, resolution_data: {}}
    9. If response 409: log.info "already resolved — idempotent ok", return True
    10. If other non-200x: log.warning, return False
    11. On success: return True
    12. Wrap entire function in try/except httpx.RequestError and broad Exception: log.error, return False

    In handle_callback, locate the section after _RESOLVED_RUNS.add(run_id) and before cached = _PAUSED_TOOLS.pop(run_id, None). Insert:

    Extract tool_call_id from callback_data (already parsed from 4-part format: action|agent_id|run_id|tool_call_id).
    Extract tg_user_id from query["from"]["id"].
    Call: resolved = await _resolve_approval_row(client, run_id, tool_call_id, confirmed, tg_user_id)
    If not resolved:
        await send_message(client, chat_id, "Approval update failed — please try again.")
        _RESOLVED_RUNS.discard(run_id)
        return

    Do not change the existing /continue call — it proceeds unchanged after successful resolve.
    Do not change _RESOLVED_RUNS.discard(run_id) in the existing error handling for /continue failure — that remains as-is.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-agents-brain && python -m pytest tests/test_telegram_adapter.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_telegram_adapter.py::TestApprovalBridge all 4 tests pass
    - All existing TestApproveDoubleTap, TestCallbackDataValidation, TestRoutingLogic tests still pass
    - channels/telegram_adapter.py contains async def _resolve_approval_row(
    - channels/telegram_adapter.py contains the _resolve_approval_row call inside handle_callback (grep: "await _resolve_approval_row" in the file)
    - pytest tests/ -x passes (full suite no regressions)
  </acceptance_criteria>
  <done>4 TestApprovalBridge tests pass, all existing adapter tests pass, full suite green, _resolve_approval_row present in handle_callback flow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram callback data → handle_callback | 4-part callback_data already validated by existing shape check; tool_call_id is the 4th segment |
| _resolve_approval_row → AgentOS HTTP | Network errors must not propagate — return False and surface Telegram error |
| resolve call → /continue call ordering | Must be sequential: resolve BEFORE continue; test asserts call order |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | Wrong approval row resolved | mitigate | Match by tool_call_id; only fall back to first row if tool_call_id absent in row data |
| T-14-05 | Denial of Service | Resolve failure leaves run paused | mitigate | If resolve returns False, release _RESOLVED_RUNS guard and send Telegram error — user can retry |
| T-14-06 | Elevation of Privilege | Double-resolve on 409 | accept | 409 treated as idempotent success; existing _RESOLVED_RUNS guard prevents double-callback execution |
| T-14-07 | Tampering | /continue runs before resolve completes | mitigate | await _resolve_approval_row is synchronous in the async flow; /continue call is below the if-not-resolved return guard |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
pytest tests/test_telegram_adapter.py::TestApprovalBridge -v  # 4 new tests green
pytest tests/test_telegram_adapter.py -v                      # all adapter tests green
pytest tests/ -x                                              # full suite no regressions
grep "await _resolve_approval_row" channels/telegram_adapter.py  # wiring present in handle_callback
</verification>

<success_criteria>
- _resolve_approval_row added to channels/telegram_adapter.py
- handle_callback calls _resolve_approval_row before /continue
- Resolve failure releases _RESOLVED_RUNS guard and sends Telegram error
- 409 on resolve proceeds to /continue (idempotent)
- TestApprovalBridge 4 tests pass; all existing adapter tests pass
- Full pytest suite passes
- APPR-02 requirement satisfied at unit test level
</success_criteria>

<output>
Create .planning/phases/14-approvals-surface-activation/14-02-SUMMARY.md when done
</output>
