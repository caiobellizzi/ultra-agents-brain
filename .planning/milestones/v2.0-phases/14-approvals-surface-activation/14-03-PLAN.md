---
phase: 14-approvals-surface-activation
plan: "03"
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - .planning/phases/14-approvals-surface-activation/VERIFICATION.md
autonomous: false
requirements:
  - APPR-01
  - APPR-02
  - APPR-03
  - OBS-01

must_haves:
  truths:
    - "Sending /ingest /tmp/uab-approval-smoke.md from Telegram creates a pending row in ai.agno_approvals with tool_name='ingest_to_vault'"
    - "GET /approvals?run_id=<run_id> returns that pending row before any button tap"
    - "Tapping Approve flips the row to status='approved' within 2 seconds and run_status leaves paused"
    - "Tapping Deny on a second run flips the row to status='rejected' within 2 seconds"
    - "AgentOS UI Approvals tab shows the pending row with tool name and arguments visible"
    - "journalctl shows path='approval' OBS log lines for create, resolve, and run_status events"
    - "VERIFICATION.md documents all 5 evidence points with output excerpts"
  artifacts:
    - path: ".planning/phases/14-approvals-surface-activation/VERIFICATION.md"
      provides: "Live VPS evidence for APPR-01, APPR-02, APPR-03, OBS-01"
      contains: "status='approved'"
  key_links:
    - from: "Telegram /ingest command"
      to: "ai.agno_approvals row"
      via: "Agno handle_agent_run_paused → create_approval_from_pause → db.create_approval"
      pattern: "ingest_to_vault"
    - from: "Telegram Approve tap"
      to: "ai.agno_approvals status='approved'"
      via: "_resolve_approval_row → POST /approvals/{id}/resolve"
      pattern: "resolved_by.*telegram"
---

<objective>
Run end-to-end live verification on the VPS to confirm APPR-01, APPR-02, APPR-03, and OBS-01 (approval path) are satisfied by the code shipped in 14-01 and 14-02. Produce VERIFICATION.md with evidence excerpts.

Purpose: Close the phase gate. Unit tests in 14-01 and 14-02 prove correctness; this plan proves the live system behaves as designed against the real Agno runtime and PostgreSQL.

Output: .planning/phases/14-approvals-surface-activation/VERIFICATION.md with DB query outputs, API responses, and journald log excerpts pasted in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-approvals-surface-activation/14-CONTEXT.md
@.planning/phases/14-approvals-surface-activation/14-VALIDATION.md
@.planning/phases/14-approvals-surface-activation/14-01-SUMMARY.md
@.planning/phases/14-approvals-surface-activation/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy 14-01 and 14-02 changes to VPS</name>
  <files>.planning/phases/14-approvals-surface-activation/VERIFICATION.md</files>
  <read_first>
    - .planning/phases/14-approvals-surface-activation/14-01-SUMMARY.md (confirm 14-01 shipped)
    - .planning/phases/14-approvals-surface-activation/14-02-SUMMARY.md (confirm 14-02 shipped)
    - deploy/litellm/config.yaml (confirm VPS deploy procedure — check for Makefile or systemd restart steps)
    - channels/SMOKE.md (existing smoke procedure and VPS SSH target)
  </read_first>
  <action>
    1. Confirm both 14-01 and 14-02 are committed to main (git log --oneline -5).
    2. SSH to VPS and git pull to pick up the new commits.
    3. Create smoke fixture on VPS: echo "approval smoke test $(date)" > /tmp/uab-approval-smoke.md
    4. Restart the uab-brain.service systemd unit so the new approval_recorder patch takes effect: sudo systemctl restart uab-brain.service
    5. Verify service is running: sudo systemctl status uab-brain.service — confirm Active: active (running)
    6. Create initial VERIFICATION.md stub at .planning/phases/14-approvals-surface-activation/VERIFICATION.md with sections: APPR-01, APPR-02 Approve, APPR-02 Deny, APPR-03, OBS-01. Leave evidence slots as [PENDING].
  </action>
  <verify>
    <automated>git log --oneline -5 | grep -E "14-01|14-02|approval"</automated>
  </verify>
  <acceptance_criteria>
    - git log shows commits from 14-01 and 14-02 on main branch
    - VPS uab-brain.service reports Active: active (running) after restart
    - /tmp/uab-approval-smoke.md exists on VPS with non-empty content
    - .planning/phases/14-approvals-surface-activation/VERIFICATION.md stub created locally with section headers
  </acceptance_criteria>
  <done>VPS running latest code with approval_recorder patched, smoke fixture ready, VERIFICATION.md stub created</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Deployed agentos/approval_recorder.py and channels/telegram_adapter.py (_resolve_approval_row) to VPS. Smoke fixture at /tmp/uab-approval-smoke.md is ready.
    
    Claude has prepared the VERIFICATION.md stub and confirmed the service is running. Now the live approval flow must be exercised to gather evidence.
  </what-built>
  <how-to-verify>
    Execute the following steps and paste the evidence excerpts into VERIFICATION.md:

    STEP 1 — APPR-01: Pending row creation
    - Send in Telegram: /ingest /tmp/uab-approval-smoke.md
    - BEFORE tapping any button, capture the run_id from the paused Telegram message (look for the Approve/Deny buttons)
    - Run: curl -s "http://localhost:7777/v1/approvals?run_id=<run_id>&status=pending" | python3 -m json.tool
    - Expected: one row with tool_name='ingest_to_vault', status='pending', tool_args containing the fixture path
    - Also run: psql $POSTGRES_DSN -c "SELECT id, agent_id, run_id, tool_name, status, run_status FROM ai.agno_approvals WHERE status='pending' ORDER BY created_at DESC LIMIT 1;"
    - Paste both outputs under APPR-01 section

    STEP 2 — APPR-03: UI visibility
    - Open https://os.agno.com → Approvals tab while row is still pending
    - Confirm ingest_to_vault and the fixture path are visible
    - Note: screenshot not required — text confirmation is sufficient

    STEP 3 — APPR-02 Approve path
    - Tap the Approve button in Telegram for the pending run
    - Wait 2 seconds, then run:
      psql $POSTGRES_DSN -c "SELECT id, status, run_status, resolved_by FROM ai.agno_approvals WHERE run_id='<run_id>';"
    - Expected: status='approved', resolved_by='telegram:<your_user_id>', run_status not 'paused'
    - Also confirm Telegram received a completion message (tool result or done message)
    - Paste psql output under APPR-02 Approve section

    STEP 4 — APPR-02 Deny path  
    - Create a second fixture: echo "deny smoke test $(date)" > /tmp/uab-denial-smoke.md (on VPS)
    - Send in Telegram: /ingest /tmp/uab-denial-smoke.md
    - Tap Deny
    - Run psql check: SELECT id, status, run_status FROM ai.agno_approvals WHERE status='rejected' ORDER BY created_at DESC LIMIT 1;
    - Expected: status='rejected', run_status not 'paused'
    - Paste output under APPR-02 Deny section

    STEP 5 — OBS-01: Structured logs
    - Run: sudo journalctl -u uab-brain.service --since "10 minutes ago" | grep "path.*approval" | head -20
    - Expected: JSON log lines with path='approval', op='create', op='resolve', op='run_status'
    - Paste 3+ representative log lines under OBS-01 section

    IF STEP 1 produces an empty approval list (no pending row after /ingest trigger):
    - This is the RC-hitl-write-broken condition from D-12
    - Do NOT proceed to approval resolution steps
    - Document the failure in VERIFICATION.md under "HITL Write Broken" heading
    - Include: the Telegram response text, the psql output showing ai.agno_approvals row count, and any relevant journald errors
    - Stop and report — do not attempt workarounds
  </how-to-verify>
  <resume-signal>
    Paste the evidence excerpts (curl/psql/journald outputs) from all 5 steps into VERIFICATION.md and type "verified" or describe what failed (including RC-hitl-write-broken if applicable).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize VERIFICATION.md with evidence and update REQUIREMENTS.md</name>
  <files>.planning/phases/14-approvals-surface-activation/VERIFICATION.md</files>
  <read_first>
    - .planning/phases/14-approvals-surface-activation/VERIFICATION.md (current state with pasted evidence)
    - .planning/REQUIREMENTS.md (APPR-01, APPR-02, APPR-03, OBS-01 checkbox status)
    - .planning/phases/14-approvals-surface-activation/14-VALIDATION.md (per-task verification map — mark 14-03-01 through 14-03-05 as green)
  </read_first>
  <action>
    1. Finalize VERIFICATION.md: replace any [PENDING] placeholders with the evidence provided in the checkpoint. Add a summary section at the top listing each requirement ID and its verification status (PASS / FAIL / PARTIAL).
    2. In .planning/REQUIREMENTS.md: mark APPR-01, APPR-02, APPR-03 as [x] (checked). OBS-01 is already [x] from phase 11; confirm approval path is noted.
    3. In .planning/phases/14-approvals-surface-activation/14-VALIDATION.md: update tasks 14-03-01 through 14-03-05 from ⬜ pending to ✅ green (or ❌ red with notes for any failures).
    4. If any check failed, note it clearly in VERIFICATION.md but do not alter the REQUIREMENTS.md checkbox for that requirement — leave it unchecked and note the open issue.
  </action>
  <verify>
    <automated>grep -c "PASS\|FAIL\|PARTIAL" .planning/phases/14-approvals-surface-activation/VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - VERIFICATION.md contains at least one line with "PASS" or "FAIL" for each of APPR-01, APPR-02, APPR-03, OBS-01
    - VERIFICATION.md does not contain any [PENDING] placeholders
    - .planning/REQUIREMENTS.md APPR-01, APPR-02, APPR-03 lines are updated to [x] if evidence confirms PASS
    - .planning/phases/14-approvals-surface-activation/14-VALIDATION.md 14-03-xx rows updated from ⬜ to ✅ or ❌
  </acceptance_criteria>
  <done>VERIFICATION.md complete with all evidence; REQUIREMENTS.md reflects verified status; VALIDATION.md updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VPS live run → ai.agno_approvals table | Real tool execution; smoke fixture uses non-sensitive content only (D-10) |
| psql query output → VERIFICATION.md | DB output pasted as text — check for sensitive data before pasting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Information Disclosure | psql output in VERIFICATION.md | mitigate | Use non-sensitive fixture content (D-10); review psql output for unexpected sensitive fields before pasting |
| T-14-09 | Repudiation | RC-hitl-write-broken not caught | mitigate | Explicit STOP instruction if GET /approvals returns empty after trigger; document in VERIFICATION.md |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
grep "APPR-01\|APPR-02\|APPR-03\|OBS-01" .planning/phases/14-approvals-surface-activation/VERIFICATION.md
grep "status='approved'\|status='rejected'" .planning/phases/14-approvals-surface-activation/VERIFICATION.md
grep "\[x\]" .planning/REQUIREMENTS.md | grep "APPR"
</verification>

<success_criteria>
- VERIFICATION.md created with live DB/API/log evidence for all 4 requirements
- APPR-01: pending row confirmed via GET /approvals and psql
- APPR-02: both approve (status=approved) and deny (status=rejected) paths documented
- APPR-03: tool_name='ingest_to_vault' and args visible in approval row
- OBS-01: journald log lines with path='approval' and op=create/resolve/run_status present
- REQUIREMENTS.md APPR-01, APPR-02, APPR-03 checked off
- Phase 14 complete
</success_criteria>

<output>
Create .planning/phases/14-approvals-surface-activation/14-03-SUMMARY.md when done
</output>
