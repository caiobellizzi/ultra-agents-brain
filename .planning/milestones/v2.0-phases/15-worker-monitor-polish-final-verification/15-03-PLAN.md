---
phase: "15-worker-monitor-polish-final-verification"
plan: "15-03"
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - scripts/check_surfaces.py
  - Makefile
  - RETROSPECTIVE.md
autonomous: true
requirements: [OBS-02]
must_haves:
  truths:
    - "`scripts/check_surfaces.py` exists and is executable"
    - "`Makefile` contains a `check-surfaces` target that runs `scripts/check_surfaces.py`"
    - "Running `make check-surfaces` with `POSTGRES_DSN_SESSIONS` set prints row counts for memory, evals, knowledge (vault), and approvals surfaces"
    - "Running `make check-surfaces` without `POSTGRES_DSN_SESSIONS` prints a warning and exits 0 (graceful local fallback)"
    - "`RETROSPECTIVE.md` exists at repo root with non-empty content covering: milestone timeline (phases 10–18), key decisions, known gaps, lessons"
  artifacts:
    - path: "scripts/check_surfaces.py"
      provides: "OBS-02 smoke checker"
      min_lines: 40
    - path: "Makefile"
      provides: "check-surfaces target"
    - path: "RETROSPECTIVE.md"
      provides: "v2.0 retrospective"
      min_lines: 50
---

# Plan 15-03 — check-surfaces + RETROSPECTIVE.md (OBS-02)

## Objective

Deliver two artifacts: (1) a `make check-surfaces` smoke script that queries row counts for all 4 AgentOS surfaces; (2) a `RETROSPECTIVE.md` documenting the v2.0 milestone.

---

## Task 1 — Create `scripts/check_surfaces.py`

<read_first>
- `agentos/db.py` — read in full (understand `POSTGRES_DSN_SESSIONS`, `POSTGRES_DB`, `PostgresDb(id="ultra-brain-main")`)
- `agentos/knowledge.py` — read the `make_knowledge()` function to find `table_name="vault"` and the `POSTGRES_DSN_KNOWLEDGE` env var
- `agentos/app.py` — lines 1–60 (understand which env vars govern which surfaces)
- `.venv/lib/python3.13/site-packages/agno/db/postgres/postgres.py` — read lines 220–250 to understand `session_table_name`, `memory_table_name`, `eval_table_name`, `approvals_table_name` attribute names on `PostgresDb`
</read_first>

<action>
Create `scripts/check_surfaces.py` (make executable with `chmod +x`). The script:

1. Reads `POSTGRES_DSN_SESSIONS` and `POSTGRES_DSN_KNOWLEDGE` from env
2. If `POSTGRES_DSN_SESSIONS` is not set: print `[local mode] POSTGRES_DSN_SESSIONS not set — skipping memory/evals/approvals check` and exit 0
3. If set, instantiate `PostgresDb(id="ultra-brain-main", db_url=POSTGRES_DSN_SESSIONS)` using imports from `agentos.db` module path (not direct agno imports) to get the correct table name constants
4. Query row counts for the four surfaces using the `PostgresDb` instance's `session_table_name`, `memory_table_name`, `eval_table_name`, `approvals_table_name` attributes (do NOT hardcode table names)
5. For knowledge: use `POSTGRES_DSN_KNOWLEDGE` (may differ from sessions DSN) and query the `vault` table directly via `psycopg2` or `sqlalchemy`
6. Print each surface with count and ✓/✗:
   ```
   memory:    ✓  42 rows   (ultra_brain_main_memories)
   evals:     ✓  7 rows    (ultra_brain_main_evals)
   knowledge: ✓  156 rows  (vault)
   approvals: ✓  3 rows    (ultra_brain_main_approvals)
   ```
7. Exit 1 if any surface has 0 rows; exit 0 if all non-zero

If a table doesn't exist yet (TableNotFoundError or `psycopg2.errors.UndefinedTable`), print `✗ TABLE MISSING` for that surface and count it as a failure.

Import pattern: import from `agno.db.postgres import PostgresDb` (already in `.venv`); use `psycopg2` for the raw vault count (already a transitive dependency via `agno`).
</action>

<acceptance_criteria>
- `scripts/check_surfaces.py` exists
- `python scripts/check_surfaces.py` with no env vars prints local-mode warning and exits 0
- Script contains no hardcoded table name strings — only references to `db.session_table_name`, `db.memory_table_name`, `db.eval_table_name`, `db.approvals_table_name` attribute reads
- The vault count uses `vault` as the table name (match `agentos/knowledge.py:126`)
</acceptance_criteria>

---

## Task 2 — Add `check-surfaces` Makefile target

<read_first>
- `Makefile` — read the full file (understand existing targets: `test`, `eval-smoke`, `eval-full`)
</read_first>

<action>
Append to `Makefile` after the existing targets:

```makefile
check-surfaces:
	PYTHONPATH=. .venv/bin/python scripts/check_surfaces.py
```

Keep the same indentation convention (tab, not spaces) as existing targets.
</action>

<acceptance_criteria>
- `Makefile` contains `check-surfaces:` target
- `make check-surfaces` runs without error when POSTGRES_DSN_SESSIONS is unset (exits 0 in local mode)
- `cat Makefile | grep check-surfaces` returns the target line
</acceptance_criteria>

---

## Task 3 — Write `RETROSPECTIVE.md`

<read_first>
- `.planning/ROADMAP.md` — read the v2.0 milestone section (phases 10–18 with goals and success criteria)
- `.planning/STATE.md` — read for current status and completed phase notes
- `.planning/REQUIREMENTS.md` — read for the full requirements list and completion status
</read_first>

<action>
Create `RETROSPECTIVE.md` at the repo root. Include:

```markdown
# ultra-agents-brain v2.0 Retrospective

**Milestone:** AgentOS Surface Activation
**Phases:** 10–18 (9 phases)
**Period:** ~May 2026

## What shipped

| Phase | Name | Status | Key deliverable |
|-------|------|--------|-----------------|
| 10 | AgentOS Migration + PostgreSQL | ✓ | SQLite → Postgres, AgentOS host |
| 11 | Memory Surface Activation | ✓ | InstrumentedMemoryManager, OBS-01 logging |
| 12 | Evals Surface Activation | ✓ | eval_recorder live patches, live judge worker |
| 13 | Knowledge Surface Activation | ✓ | pgvector vault reindex, make_knowledge wiring |
| 14 | Approvals Surface Activation | ✓ | Telegram HITL inline buttons, callback_data auth |
| 15 | worker.monitor Polish + Final Verification | ✓ | MON-01 date-mismatch fix, MON-02 regression tests, check-surfaces |
| 16 | Brain Vault Overhaul | ✓ | TELOS fill, inbox sweep, vault registry |
| 17 | Multi-repo brain pipelines | ✓ | GitHub Actions reusable workflows |
| 18 | Auto-sync second-brain | ✓ | VPS deploy key, git-sync.sh, launchd sync |

## Key decisions

- **SQLite → PostgreSQL (Phase 10):** Needed for pgvector, AgentOS dashboard, and multi-process access on VPS. Agno's SqliteDb has no migration story.
- **Single shared PostgresDb (Phase 13):** `POSTGRES_DB` singleton in `agentos/db.py` — avoids N connections and ensures knowledge + memory see the same schema.
- **eval_recorder via class-level monkey-patch (Phase 12):** Patches `Agent.run` / `Agent.arun` at import time so all agents are instrumented without code changes. Known limitation: streaming runs not recorded.
- **Telegram HITL via callback_data (Phase 14):** Agno's native `requires_confirmation=True` wired to inline keyboard buttons. UUID validation prevents arbitrary callback injection.
- **2-pass vault sync (Phase 18):** Pull-first strategy (VPS → Mac, no `--delete`) then push (Mac → VPS, `--delete`) protects VPS-generated Inbox items from deletion.
- **TELOS scoring for inbox routing (Phase 16):** Items >= 0.6 → articles/, < 0.3 → auto-culled/, 0.3–0.6 → Inbox (read by brief).

## Known gaps not closed

- `model_id` is null in all live eval rows (Agno `RunOutput.model` returns tier name string, not typed Model object with `.id`)
- Streaming and background agent runs bypass eval_recorder entirely
- `check-surfaces` approvals count likely 0 in production (no approval requests generated since Phase 14)

## Surprises

- Phase 18 required a VPS deploy key as a prerequisite not in the original plan — added as Phase 18-01
- iCloud APFS `.unlink()` fails silently on mounted shares; had to switch to `move_to_trash()` (Phase 16-05)
- `date.today()` mismatch between monitor and brief crons caused all evening-filed items to be missed by next morning's brief (MON-01, Phase 15)

## Lessons

- `/gsd:discuss-phase` before each phase produced tighter, better-verified plans — phases with CONTEXT.md executed faster
- Nyquist validation gaps found real missing tests in phases 12–14
- VPS env var loading order matters: `set -a; source .env; set +a` needed before subprocess spawns
```
</action>

<acceptance_criteria>
- `RETROSPECTIVE.md` exists at repo root
- `wc -l RETROSPECTIVE.md` returns >= 50
- File contains "Phase 10" through "Phase 18" references
- File contains at least one entry in "Known gaps" and "Lessons" sections
</acceptance_criteria>
