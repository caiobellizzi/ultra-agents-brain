---
phase: 12-evals-surface-activation
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - .planning/phases/12-evals-surface-activation/evidence/
  - .planning/phases/12-evals-surface-activation/12-VERIFICATION.md
autonomous: false
requirements: [EVAL-03, EVAL-01, EVAL-02, OBS-01]
user_setup:
  - Operator must have redeployed `uab-brain.service` on the VPS after plan 12-01 shipped (so the running process carries `InstrumentedEvalRecorder`).
  - Operator must have `POSTGRES_DSN_SESSIONS`, `EVAL_JUDGE_TIER`, and VPS SSH + psql access available.
  - Operator must be logged in at https://os.agno.com with access to the Evals tab for the `ultra-brain-main` db.

must_haves:
  decisions:
    - "D-12: EVAL-03 two-tier smoke procedure (private-worker then orchestrator) with row-count + model_id evidence — tasks 2 and 3."
    - "D-13: closeout plan in the 3-plan split — this is plan 3 of 3."
    - "D-15: OBS-01 (eval path) log schema verified live with all 11 required fields — task 5 captures fault-injection JSON evidence; task 1 verifies success-path journal lines."
  truths:
    - VERIFICATION.md exists with `status: passed` once all six checks (V1–V6 from RESEARCH.md) are green.
    - Evidence files exist under `.planning/phases/12-evals-surface-activation/evidence/` for the two-tier smoke and the dashboard snapshot.
    - At least one `AGENT_AS_JUDGE` row and one `ACCURACY` row visible via `GET /eval-runs?db_id=ultra-brain-main`.
    - Operator confirms the os.agno.com Evals tab renders both row types.
    - One `status="error"` OBS-01 log line exists in journalctl, generated by a deliberate fault-injection (toggle the wrapper to point at an unreachable DB momentarily, OR run `pytest tests/unit/test_eval_recorder.py::test_wrap_swallows_db_error -q` on the VPS — the test logs through the same `agentos.eval` logger).
  artifacts:
    - .planning/phases/12-evals-surface-activation/evidence/eval-live-traffic-2026-05-XX.md
    - .planning/phases/12-evals-surface-activation/evidence/eval-tier-swap-2026-05-XX.md
    - .planning/phases/12-evals-surface-activation/evidence/eval-dashboard-2026-05-XX.png
    - .planning/phases/12-evals-surface-activation/12-VERIFICATION.md
  key_links:
    - Closes EVAL-01 (live), EVAL-02 (suite), EVAL-03 (tier swap), OBS-01 (eval path) all in one verification pass.
    - Mirrors plan 11-03's closeout structure (smoke evidence + SUMMARY + VERIFICATION).
---

<objective>
End-to-end verification + closeout for Phase 12. No new code. Operator-driven smoke procedure proving:

1. Live agent runs write `AGENT_AS_JUDGE` rows (EVAL-01).
2. The 48-case suite writes `ACCURACY` rows (EVAL-02).
3. Swapping `EVAL_JUDGE_TIER` produces rows with a distinct `model_id` (EVAL-03).
4. OBS-01 (eval path) emits structured log lines on success and failure (OBS-01).
5. `GET /eval-runs` returns the rows; os.agno.com Evals tab renders them.

Output: `12-VERIFICATION.md` with `status: passed` and three evidence files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-evals-surface-activation/12-CONTEXT.md
@.planning/phases/12-evals-surface-activation/12-RESEARCH.md
@.planning/phases/12-evals-surface-activation/12-VALIDATION.md
@.planning/phases/11-memory-surface-activation/11-03-PLAN.md
@.planning/phases/11-memory-surface-activation/11-03-SUMMARY.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: VPS redeploy + live-traffic smoke (EVAL-01)</name>
  <files>
    .planning/phases/12-evals-surface-activation/evidence/eval-live-traffic-2026-05-XX.md
  </files>
  <read_first>
    - .planning/phases/12-evals-surface-activation/12-01-PLAN.md (what shipped in 12-01)
    - .planning/phases/11-memory-surface-activation/11-03-SUMMARY.md (the format to mirror)
  </read_first>
  <action>
    1. On the VPS: `git pull` then `systemctl restart uab-brain.service`. Confirm it came up: `systemctl status uab-brain.service` shows `active (running)` and the journal shows no startup error.
    2. Send one real Telegram message to the chat agent (any message — "hi from phase 12 verification"). Within 5 seconds:
       ```bash
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT run_id, agent_id, eval_type, eval_data->>'model_id' AS model, eval_data->>'status' AS status, created_at FROM ai.agno_eval_runs WHERE eval_type = 'agent_as_judge' ORDER BY created_at DESC LIMIT 3;"
       ```
       Expect ≥1 row created in the last 5 s, `status='ok'`, `model` matches `default-worker` tier.
    3. Confirm the API surface:
       ```bash
       curl -s "${BRAIN_URL}/eval-runs?db_id=ultra-brain-main&eval_type=agent_as_judge" | jq '.data | length'
       ```
       Expect ≥1.
    4. Tail the journal for OBS-01:
       ```bash
       journalctl -u uab-brain.service -n 200 | grep '"path":"eval"' | tail -3 | jq .
       ```
       Expect well-formed JSON with all D-15 fields.
    5. Write evidence file `evidence/eval-live-traffic-2026-05-XX.md` (use today's date) with: redeploy timestamp, Telegram message text, the psql output, the curl output, and the journal snippet.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/evidence/eval-live-traffic-*.md && \
      grep -q "agent_as_judge" .planning/phases/12-evals-surface-activation/evidence/eval-live-traffic-*.md
    </automated>
    <human-check>
      Operator visually confirms the evidence file shows the psql row count, the curl returned ≥1, and the journal JSON has all D-15 fields.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Evidence file `evidence/eval-live-traffic-2026-05-XX.md` exists and contains: redeploy command + output, telegram message text, psql query + ≥1 returned row, curl command + JSON length ≥1, journal grep + parsed JSON with `path="eval"` and `status="ok"`.
    - `SELECT count(*) FROM ai.agno_eval_runs WHERE eval_type='agent_as_judge' AND created_at > extract(epoch from now()-interval '10 minutes')::int` returns ≥1.
  </acceptance_criteria>
  <done>
    EVAL-01 + OBS-01 (eval path) confirmed live on the VPS.
  </done>
</task>

<task type="execute">
  <name>Task 2: Run the full eval suite — first tier (EVAL-02)</name>
  <files>
    .planning/phases/12-evals-surface-activation/evidence/eval-suite-private-worker-2026-05-XX.md
  </files>
  <read_first>
    - evals/conftest.py (the new `eval_recorder` fixture + hook from plan 12-02)
    - evals/test_*.py (existing suite — see how many cases run)
  </read_first>
  <action>
    1. Open at least one suite test file (e.g. `evals/test_chat.py`) and confirm it depends on the new `eval_recorder` fixture and calls `eval_recorder(score, output, eval_input)` at the end of each parametrized case. If NO test file does — that is expected for first-shipment of 12-02. In that case, this task EXTENDS one suite file (the smallest, typically `evals/test_curator.py`) to call the recorder, runs the suite, and counts the rows. The other suite files adopt the pattern in follow-up activity per CONTEXT D-13 (12-03 ships the proof; full-suite adoption is incremental).

    2. Snapshot the table row count before the run:
       ```bash
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT count(*) FROM ai.agno_eval_runs WHERE eval_type='accuracy'" > /tmp/before.txt
       ```
    3. Run the suite with the default tier:
       ```bash
       POSTGRES_DSN_SESSIONS=$POSTGRES_DSN_SESSIONS \
       EVAL_JUDGE_TIER=private-worker \
       pytest evals/ -q -m "not live" 2>&1 | tee /tmp/eval-suite-private-worker.log
       ```
    4. Snapshot after:
       ```bash
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT count(*) FROM ai.agno_eval_runs WHERE eval_type='accuracy'" > /tmp/after.txt
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT DISTINCT eval_data->>'model_id' FROM ai.agno_eval_runs WHERE eval_type='accuracy' AND created_at > extract(epoch from now()-interval '30 minutes')::int"
       ```
    5. Write evidence `evidence/eval-suite-private-worker-2026-05-XX.md` with: row count delta, the model_id list (expect one or more identifiers matching the `private-worker` tier — LM Studio model), and the pytest summary line.

    Confirm row delta ≥ (number of cases run in the file that adopted the recorder). For just `evals/test_curator.py` that's typically 3-5 cases.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/evidence/eval-suite-private-worker-*.md && \
      grep -qE "row count delta|new rows" .planning/phases/12-evals-surface-activation/evidence/eval-suite-private-worker-*.md
    </automated>
    <human-check>
      Operator confirms the row count delta is positive and the model_id list contains only `private-worker` tier identifiers.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Evidence file `evidence/eval-suite-private-worker-2026-05-XX.md` exists with row-count delta ≥ N (where N is the number of cases that called `eval_recorder`).
    - `SELECT DISTINCT eval_data->>'model_id' FROM ai.agno_eval_runs WHERE eval_type='accuracy' AND created_at > extract(epoch from now()-interval '1 hour')::int` returns the private-worker tier model ids.
    - At least one suite test file (e.g. `evals/test_curator.py`) has been updated to call `eval_recorder(...)` at the end of each case.
  </acceptance_criteria>
  <done>
    EVAL-02 confirmed — accuracy rows landed under tier 1.
  </done>
</task>

<task type="execute">
  <name>Task 3: Run the eval suite — second tier (EVAL-03)</name>
  <files>
    .planning/phases/12-evals-surface-activation/evidence/eval-tier-swap-2026-05-XX.md
  </files>
  <read_first>
    - .planning/phases/12-evals-surface-activation/evidence/eval-suite-private-worker-2026-05-XX.md (the first-tier baseline)
    - agentos/model.py (`chat_model(EVAL_JUDGE_TIER)` dispatch — confirm tier 2 model identifier)
  </read_first>
  <action>
    1. Snapshot the accuracy row count before:
       ```bash
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT count(*) FROM ai.agno_eval_runs WHERE eval_type='accuracy'"
       ```
    2. Re-run the SAME suite file as task 2 with the second tier:
       ```bash
       POSTGRES_DSN_SESSIONS=$POSTGRES_DSN_SESSIONS \
       EVAL_JUDGE_TIER=orchestrator \
       pytest evals/test_curator.py -q -m "not live" 2>&1 | tee /tmp/eval-suite-orchestrator.log
       ```
    3. Snapshot after + collect distinct model_ids from this run:
       ```bash
       psql "$POSTGRES_DSN_SESSIONS" -c "SELECT DISTINCT eval_data->>'model_id' FROM ai.agno_eval_runs WHERE eval_type='accuracy' AND created_at > extract(epoch from now()-interval '15 minutes')::int"
       ```
    4. Assert the model_id set from this run is DISJOINT from the set captured in task 2 (the `private-worker` tier returned, e.g., `lmstudio/qwen-...`; the `orchestrator` tier should return, e.g., `anthropic/claude-...` or whatever `chat_model("orchestrator")` resolves to).
    5. Write evidence `evidence/eval-tier-swap-2026-05-XX.md` with: both row-count deltas, both model_id sets, an explicit "disjoint: yes/no" assertion. If "no", investigation required — file findings inline and re-run.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/evidence/eval-tier-swap-*.md && \
      grep -q "disjoint: yes" .planning/phases/12-evals-surface-activation/evidence/eval-tier-swap-*.md
    </automated>
    <human-check>
      Operator visually confirms two distinct model_id sets, no overlap.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Evidence file `evidence/eval-tier-swap-2026-05-XX.md` exists with row-count delta + two model_id sets + explicit `disjoint: yes` assertion.
    - The two model_id sets share no common element (verified by the operator visually OR by `comm -12 <(sort tier1.txt) <(sort tier2.txt)` returning empty).
  </acceptance_criteria>
  <done>
    EVAL-03 confirmed — tier-swap proven end-to-end with distinct judge models.
  </done>
</task>

<task type="execute">
  <name>Task 4: Dashboard render verification (EVAL-01 + EVAL-02 surface)</name>
  <files>
    .planning/phases/12-evals-surface-activation/evidence/eval-dashboard-2026-05-XX.png
  </files>
  <read_first>
    - .planning/phases/12-evals-surface-activation/evidence/eval-live-traffic-*.md
    - .planning/phases/12-evals-surface-activation/evidence/eval-suite-*-2026-05-XX.md
  </read_first>
  <action>
    1. Operator opens https://os.agno.com → log in → select the `ultra-brain-main` db.
    2. Navigate to the Evals tab.
    3. Confirm at least one row of each `eval_type` is rendered (`agent_as_judge` from task 1, `accuracy` from tasks 2+3).
    4. Take a screenshot of the dashboard showing both row types. Save as `evidence/eval-dashboard-2026-05-XX.png`.
    5. If the dashboard does NOT render the rows but psql + `/eval-runs` confirms the rows exist, file the symptom in `evidence/eval-dashboard-2026-05-XX.md` and continue — the dashboard rendering is a SaaS-side concern. The phase still passes if the API + psql checks confirm the data is there.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/evidence/eval-dashboard-*
    </automated>
    <human-check>
      Operator confirms the screenshot shows both `agent_as_judge` and `accuracy` rows (OR a written explanation if the dashboard is broken).
    </human-check>
  </verify>
  <acceptance_criteria>
    - A file matching `evidence/eval-dashboard-2026-05-XX.*` (PNG or MD) exists under the phase evidence directory.
  </acceptance_criteria>
  <done>
    Operator-confirmed dashboard surface.
  </done>
</task>

<task type="execute">
  <name>Task 5: OBS-01 failure-path evidence</name>
  <files>
    .planning/phases/12-evals-surface-activation/evidence/eval-obs01-failure-2026-05-XX.md
  </files>
  <read_first>
    - tests/unit/test_eval_recorder.py::test_wrap_swallows_db_error (the controlled fault-injection)
    - .planning/phases/12-evals-surface-activation/12-RESEARCH.md §V2
  </read_first>
  <action>
    1. On the VPS (or any machine with the prod logger config), run:
       ```bash
       cd /opt/uab && .venv/bin/pytest tests/unit/test_eval_recorder.py::test_wrap_swallows_db_error -q -o log_cli=true -o log_cli_level=ERROR 2>&1 | tee /tmp/eval-obs01-fail.log
       ```
    2. Confirm the captured log line is well-formed JSON: `grep "OBS-01 eval write failed" /tmp/eval-obs01-fail.log` returns one line; parse the JSON after the prefix with `jq -R 'fromjson? // .'`.
    3. Write evidence `evidence/eval-obs01-failure-2026-05-XX.md` with the parsed JSON and a note explaining the controlled fault-injection.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/evidence/eval-obs01-failure-*.md && \
      grep -qE '"path":\s*"eval"' .planning/phases/12-evals-surface-activation/evidence/eval-obs01-failure-*.md && \
      grep -qE '"status":\s*"error"' .planning/phases/12-evals-surface-activation/evidence/eval-obs01-failure-*.md
    </automated>
    <human-check>
      Operator confirms the JSON line carries every D-15 field (path, status, error_type, error_msg).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Evidence file exists containing the parsed OBS-01 error JSON.
    - The JSON contains `path="eval"`, `status="error"`, `error_type`, `error_msg`.
  </acceptance_criteria>
  <done>
    OBS-01 (eval path) failure-side proven.
  </done>
</task>

<task type="execute">
  <name>Task 6: Write VERIFICATION.md and SUMMARY.md, close phase 12</name>
  <files>
    .planning/phases/12-evals-surface-activation/12-VERIFICATION.md,
    .planning/phases/12-evals-surface-activation/12-03-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/12-evals-surface-activation/evidence/*.md
    - .planning/phases/11-memory-surface-activation/11-03-SUMMARY.md (closeout template)
  </read_first>
  <action>
    1. Write `12-VERIFICATION.md` with YAML frontmatter `status: passed` (or `status: failed` if any check above failed), listing each of the six verification protocol items from CONTEXT.md §verification_protocol with a ✓ / ✗ and a link to the relevant evidence file.

       ```yaml
       ---
       phase: 12
       slug: evals-surface-activation
       status: passed
       must_haves_total: 7
       must_haves_passed: 7
       verified: 2026-05-XX
       ---
       ```

       Body sections:
       - Verification protocol checklist (6 items, ✓/✗, evidence links).
       - Must-haves table from CONTEXT.md (count passed vs total).
       - Open items (none — or list deferred work per CONTEXT.md §deferred).

    2. Write `12-03-SUMMARY.md` covering what shipped across all 3 plans + the verification result. Mirror `11-03-SUMMARY.md` format: what changed, evidence pointers, follow-ups.

    3. If verification status is `failed`, file a fix-up plan request (do NOT close the phase). Otherwise, no further action.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12-evals-surface-activation/12-VERIFICATION.md && \
      grep -qE "status:\s*passed|status:\s*failed" .planning/phases/12-evals-surface-activation/12-VERIFICATION.md && \
      test -f .planning/phases/12-evals-surface-activation/12-03-SUMMARY.md
    </automated>
    <human-check>
      Operator reads VERIFICATION.md and confirms each of the six checks links to a real evidence file.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `12-VERIFICATION.md` exists with valid YAML frontmatter containing `status: passed` (or `failed` with explanation).
    - All six verification-protocol items from CONTEXT.md are listed with ✓/✗ and evidence-file links.
    - `12-03-SUMMARY.md` exists summarising what shipped.
  </acceptance_criteria>
  <done>
    Phase 12 is closed. Roadmap can advance to phase 13 (Knowledge Surface).
  </done>
</task>

</tasks>

<threat_model>
| ID | Threat | Mitigation in this plan |
|----|--------|-------------------------|
| T-12-08 | Operator forgets to redeploy → live traffic check fails or returns stale rows | Task 1 step 1 makes redeploy the first step and verifies `systemctl status` before running smoke. |
| T-12-09 | First-tier and second-tier resolve to the SAME model (config error) → EVAL-03 falsely passes | Task 3 step 4 asserts the model_id sets are disjoint; operator visually confirms the two distinct identifiers in the evidence file. |
| T-12-10 | Dashboard shows nothing but psql shows rows → false "broken" closeout | Task 4 step 5 explicitly allows the operator to document a SaaS-side failure and still pass the phase if API + psql confirm data presence. |
| T-12-11 | Verification status accidentally written as `passed` despite a failed sub-check | Task 6 acceptance criterion checks the frontmatter pattern; human-check explicitly asks the operator to confirm every link resolves. |
</threat_model>

<rollback>
Plan 12-03 produces only documentation + evidence files. To roll back:
- Delete `12-VERIFICATION.md`, `12-03-SUMMARY.md`, and `evidence/*` from the phase directory.
- The code shipped in 12-01 and 12-02 is rolled back via their respective rollback sections (independent reverts).
- The eval rows already written to `ai.agno_eval_runs` can be cleaned up if needed:
  `DELETE FROM ai.agno_eval_runs WHERE created_at > extract(epoch from '2026-05-23'::date)::int;`
  (Not required — keeping them as historical record is the design intent.)
</rollback>
