---
phase: 10-diagnostic-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-diagnostic-audit/AUDIT.md
  - .planning/phases/10-diagnostic-audit/evidence/
autonomous: false
requirements: [DIAG-01]
user_setup:
  - Provide VPS SSH access (existing) so audit tasks can run docker exec / psql against the production stack.
  - Provide the AgentOS JWT (or confirm it's already in 1Password / .env) so curl probes against the deployed AgentOS can authenticate. Do not commit the token; reference it as `$AGENTOS_JWT` only.
  - Operator opens https://os.agno.com in a browser to capture UI state and devtools network traces for each of the 4 surfaces.

must_haves:
  truths:
    - AUDIT.md contains a top-of-document matrix table covering memory, eval, knowledge, approval — columns must match D-09.
    - Each of the 4 surfaces has a full 5-section block per D-08 (expected write path, DB schema + row evidence, AgentOS API response, UI state, root-cause hypothesis).
    - Every absence-of-evidence finding cites the exact psql query or curl command used (D-02).
    - No production write occurs during the audit (D-03 — verify by checking pg_stat write counters before/after, or by running only SELECT/`\d` queries).
  artifacts:
    - .planning/phases/10-diagnostic-audit/AUDIT.md
    - .planning/phases/10-diagnostic-audit/evidence/ (browser screenshots + raw curl response bodies)
  key_links:
    - Each AUDIT.md surface section links the Agno source file:line for the expected write function (per D-04 / D-07).
---

<objective>
Execute the read-only diagnostic audit of all four AgentOS write paths (memory, eval, knowledge, approval) end-to-end on the production VPS, and write `phases/10-diagnostic-audit/AUDIT.md` with one matrix row + 5-section block per surface, plus a `phases/10-diagnostic-audit/evidence/` directory holding raw artifacts.

Purpose: Convert prior incident speculation (S603, S605, S21808, S21815) into evidence-anchored findings that phases 11–14 can implement against without re-discovering. The audit is the contract between this diagnostic phase and the four surface-activation phases that follow.

Output: Single AUDIT.md (markdown), plus a `evidence/` subdir with browser screenshots, raw curl response JSON, and psql output captures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-diagnostic-audit/10-CONTEXT.md
@.planning/phases/10-diagnostic-audit/10-RESEARCH.md
@agentos/app.py
@agentos/db.py
@deploy/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture deployment wiring + verify read-only access</name>
  <files>.planning/phases/10-diagnostic-audit/evidence/wiring.md</files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/10-RESEARCH.md (project wiring section + evidence recipes)
    - agentos/app.py (single-DB selection logic at lines 25-39, agent factory wiring 41-58, AgentOS construction 61-74)
    - agentos/db.py (SqliteDb default — used only if DSN is unset)
    - deploy/docker-compose.yml (container topology, env wiring)
    - .venv/lib/python3.13/site-packages/agno/os/utils.py:246 (get_db resolution — confirm logic before probing)
  </read_first>
  <action>
    Produce `.planning/phases/10-diagnostic-audit/evidence/wiring.md` containing the following observations, captured by running `rtk`-prefixed commands against the VPS:

    1. Container topology — output of `rtk docker compose -f deploy/docker-compose.yml ps`. Record container names, image tags, state.
    2. Environment shape (redacted) — output of `rtk docker compose -f deploy/docker-compose.yml exec agentos env | rtk grep -E 'POSTGRES_DSN|UAB_|AGENTOS_|OPENAI_|ANTHROPIC_'`. Replace any password segments with `****`. Record only KEY names and presence/absence; never raw secret values.
    3. AgentOS process check — output of `rtk docker compose -f deploy/docker-compose.yml exec agentos ps -ef | rtk grep python`. Confirm which module is running (S21798 said `agentos`, not `ultra_brain` — verify).
    4. Database backend selection — confirm whether `POSTGRES_DSN_SESSIONS` is set (PostgresDb in use) or unset (SqliteDb fallback in use). Record explicitly.
    5. AgentOS-registered `db_id` snapshot — `rtk curl -s -H "Authorization: Bearer $AGENTOS_JWT" https://<vps-host>/databases | jq .` then capture the returned id and assert `id` field is a UUID (per `BaseDb.__init__` line 56 of agno/db/base.py). Save raw JSON response under `evidence/databases.json`.
    6. Read-only guardrail — record `SELECT now();` round-trip latency and an explicit statement "No INSERT/UPDATE/DELETE/DDL executed during this audit task." Acceptance criterion below verifies pg_stat counters.

    All commands MUST be `rtk`-prefixed (project CLAUDE.md golden rule). Do not echo any password, JWT, or DSN with credentials into wiring.md or any committed file.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-diagnostic-audit/evidence/wiring.md && test -f .planning/phases/10-diagnostic-audit/evidence/databases.json</automated>
    <human-check>
      Operator inspects wiring.md and confirms: (a) no plaintext secret values present, (b) db_id captured from /databases, (c) which DB backend (Postgres vs Sqlite) is active.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `evidence/wiring.md` contains a "Container topology" section listing each docker-compose service with its state column.
    - `evidence/wiring.md` contains an "Env shape" section listing env var KEYS only — every value is either `set` / `unset` / `****`. A grep for `password|secret|token|key` against value strings returns zero matches.
    - `evidence/wiring.md` records whether the active DB backend is PostgresDb or SqliteDb, with the source-of-truth check (`POSTGRES_DSN_SESSIONS` set / unset).
    - `evidence/databases.json` exists and contains valid JSON with at least one `id` field that matches a UUID v4 pattern.
    - `evidence/wiring.md` contains the literal sentence: "No INSERT/UPDATE/DELETE/DDL executed during this audit task." and references the SELECT-only command list used.
  </acceptance_criteria>
  <done>
    Wiring captured, db_id known, secrets confirmed redacted, read-only guardrail asserted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit the four write paths — psql + curl + UI evidence per surface</name>
  <files>
    .planning/phases/10-diagnostic-audit/evidence/memory.json,
    .planning/phases/10-diagnostic-audit/evidence/evals.json,
    .planning/phases/10-diagnostic-audit/evidence/knowledge.json,
    .planning/phases/10-diagnostic-audit/evidence/approvals.json,
    .planning/phases/10-diagnostic-audit/evidence/memory-ui.png,
    .planning/phases/10-diagnostic-audit/evidence/evals-ui.png,
    .planning/phases/10-diagnostic-audit/evidence/knowledge-ui.png,
    .planning/phases/10-diagnostic-audit/evidence/approvals-ui.png,
    .planning/phases/10-diagnostic-audit/evidence/psql.txt
  </files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/10-RESEARCH.md (per-surface write-path section — has the Agno file:line refs and table names for each surface)
    - .planning/phases/10-diagnostic-audit/evidence/wiring.md (db_id from Task 1)
    - .venv/lib/python3.13/site-packages/agno/db/base.py (lines 56-72 — default table names — required for the SQL to target the right tables)
    - .venv/lib/python3.13/site-packages/agno/os/routers/memory/memory.py (memory router endpoints)
    - .venv/lib/python3.13/site-packages/agno/os/routers/evals/evals.py (eval router endpoints)
    - .venv/lib/python3.13/site-packages/agno/os/routers/knowledge/knowledge.py (knowledge router endpoints)
    - .venv/lib/python3.13/site-packages/agno/os/routers/approvals/router.py (approvals router endpoints)
    - .venv/lib/python3.13/site-packages/agno/db/sqlite/sqlite.py (only if Task 1 found SqliteDb is active — to determine which approval/eval methods are implemented)
    - agentos/agents/*.py (every agent factory — check `enable_user_memories` setting on each agent constructor)
  </read_first>
  <action>
    For each of the four surfaces — memory, evals, knowledge, approvals — execute the same three-step evidence pass and record the raw artifacts. Output is FOUR JSON files (one per surface) holding the captured curl response bodies, FOUR PNG files (one per surface) holding the os.agno.com browser screenshot, and a single `evidence/psql.txt` accumulator of all SELECT outputs.

    Per-surface protocol (READ-ONLY — D-03 strict):

    A. DB row count and one sample row. Default Agno table names per `agno/db/base.py:57-72`: `agno_memories`, `agno_eval_runs`, `agno_knowledge`, `agno_approvals`. Append to `evidence/psql.txt`:
       ```
       rtk docker compose -f deploy/docker-compose.yml exec postgres \
         psql -U <user> -d <db> -c "\d agno_<surface>"
       rtk docker compose -f deploy/docker-compose.yml exec postgres \
         psql -U <user> -d <db> -c "SELECT count(*) FROM agno_<surface>;"
       rtk docker compose -f deploy/docker-compose.yml exec postgres \
         psql -U <user> -d <db> -c "SELECT * FROM agno_<surface> ORDER BY updated_at DESC LIMIT 1;"
       ```
       If Task 1 found SqliteDb active, swap psql for `sqlite3 $UAB_DB_PATH '...'` inside the agentos container.

    B. AgentOS HTTP API response. Capture both shapes (without and with explicit db_id) to confirm or falsify the stale-cache hypothesis:
       ```
       rtk curl -s -H "Authorization: Bearer $AGENTOS_JWT" \
         "https://<vps-host>/<endpoint>" -o evidence/<surface>.json
       rtk curl -s -H "Authorization: Bearer $AGENTOS_JWT" \
         "https://<vps-host>/<endpoint>?db_id=<id-from-wiring.md>" -o evidence/<surface>-with-id.json
       ```
       Endpoints per surface:
       - memory: `GET /memories`
       - evals: `GET /evals`
       - knowledge: `GET /knowledge/content` AND `POST /knowledge/search` with body `{"query": "test"}` (dashboard uses search, not list — per RESEARCH.md)
       - approvals: `GET /approvals` (note: approvals router uses `os_db` only, not the multi-DB `dbs` dict — `db_id` does NOT apply; document this in AUDIT.md)

    C. os.agno.com UI observation. Operator logs in, navigates to each surface tab, opens browser DevTools → Network tab, takes a screenshot (saved as `evidence/<surface>-ui.png`), and captures the outbound request URL the dashboard issues — note specifically whether it sends a `db_id` query param and which value.

    D. Targeted code grep for write-trigger gates that would explain empty surfaces:
       ```
       rtk grep -rn "enable_user_memories\|requires_confirmation" agentos/ ultra_brain/ 2>/dev/null > evidence/write-gate-grep.txt
       ```
       Records whether any agent enables memories and whether any tool is decorated with `requires_confirmation=True`. Both findings inform the "root-cause hypothesis" section of AUDIT.md.

    NO production writes. NO schema mutation. NO test data injection. If any command surprises by mutating state, abort and record the surprise in evidence/wiring.md.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-diagnostic-audit/evidence/memory.json && \
      test -f .planning/phases/10-diagnostic-audit/evidence/evals.json && \
      test -f .planning/phases/10-diagnostic-audit/evidence/knowledge.json && \
      test -f .planning/phases/10-diagnostic-audit/evidence/approvals.json && \
      test -f .planning/phases/10-diagnostic-audit/evidence/psql.txt && \
      test -f .planning/phases/10-diagnostic-audit/evidence/write-gate-grep.txt
    </automated>
    <human-check>
      Operator confirms all four PNG screenshots saved, and that for at least one surface the captured curl response matches the os.agno.com UI's empty/populated state (cross-validation).
    </human-check>
  </verify>
  <acceptance_criteria>
    - 4 JSON files exist (one per surface) containing the raw AgentOS response bodies.
    - 4 PNG files exist (one per surface) under `evidence/`.
    - `evidence/psql.txt` contains, for each of the 4 tables, the literal output of `\d <table>`, `SELECT count(*) FROM <table>;`, and `SELECT * FROM <table> ORDER BY ... LIMIT 1;`. The `\d` output verifies the column schema actually exists in production.
    - `evidence/write-gate-grep.txt` exists and either lists matches or contains the explicit text "no matches found" — absence of matches is itself evidence per D-02.
    - For knowledge specifically, both `evidence/knowledge.json` (from /knowledge/content) AND a second file for the POST /knowledge/search call exist.
    - Task is read-only: a final `SELECT n_tup_ins, n_tup_upd, n_tup_del FROM pg_stat_user_tables WHERE relname LIKE 'agno_%';` snapshot taken at task start AND end shows identical values (or, for SqliteDb, file mtime is unchanged).
  </acceptance_criteria>
  <done>
    All four surfaces have raw psql + curl + UI evidence captured under evidence/, plus the write-gate grep. No prod writes detected.
  </done>
</task>

<task type="auto">
  <name>Task 3: Synthesize AUDIT.md from raw evidence</name>
  <files>.planning/phases/10-diagnostic-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/10-CONTEXT.md (D-08 5-section template, D-09 summary matrix)
    - .planning/phases/10-diagnostic-audit/10-RESEARCH.md (per-surface Agno file:line references — these must appear verbatim in AUDIT.md's "Expected write path" section per surface)
    - All artifacts under `.planning/phases/10-diagnostic-audit/evidence/` (wiring.md, *.json, psql.txt, write-gate-grep.txt, *.png references)
  </read_first>
  <action>
    Write `.planning/phases/10-diagnostic-audit/AUDIT.md` with the structure mandated by CONTEXT.md D-08 (5 sections per surface) and D-09 (top-of-document matrix table). The doc must be operator-readable and self-contained — phases 11–14 should be able to jump straight to "their" surface row + section block.

    Required structure:

    1. **Top-of-document matrix table** (D-09 columns):
       `Surface | Write fn (Agno src) | DB table | Row count | API status | UI shows? | Root-cause tag`
       Four rows: memory, eval, knowledge, approval. Root-cause tag values are short stable identifiers (e.g. `RC-stale-db-id`, `RC-memory-disabled`, `RC-no-eval-harness`, `RC-no-knowledge-dsn`, `RC-no-hitl-tool`, `RC-auth-misconfig`) — phases 11–14 will reference these tags.

    2. **One 5-section block per surface** (D-08), in the order: memory, evals, knowledge, approvals.

       Per block:
       - **Section 1 — Expected write path.** Cite Agno source file:line for the write function (e.g. `db.upsert_user_memory()` at `agno/db/postgres/postgres.py:<line>`). Also cite the project-side gating code (e.g. agent factory file:line for memory's `enable_user_memories`).
       - **Section 2 — DB schema + row evidence.** Reproduce the relevant `\d` block and SELECT count from `evidence/psql.txt`. State row count explicitly.
       - **Section 3 — AgentOS API response.** Reproduce the curl command used and the response body (or "empty array" / "404" / "401" / "503" status). Reference `evidence/<surface>.json`. For knowledge, include both /knowledge/content and POST /knowledge/search responses.
       - **Section 4 — UI state.** Describe what os.agno.com shows (populated rows, empty state with message, error banner, login wall). Link the screenshot file: `evidence/<surface>-ui.png`. Note the outbound request URL the dashboard issues (especially the `db_id` query param shape).
       - **Section 5 — Root-cause hypothesis.** Pick exactly one RC tag from the matrix, justify in 2-4 sentences citing Section 1-4 evidence. If multiple causes apply, list secondary tags but pick a single primary one.

    3. **Appendix A: Read-only guardrail attestation** — copy in the pg_stat before/after snapshot from Task 2's acceptance criterion, prove zero writes.

    4. **Appendix B: Pointer to DB-ID-DECISION.md and BACKLOG.md** — one-line each, written in plan 10-02. Use the placeholder text "(produced in plan 10-02)" — those docs do not exist yet.

    Do NOT include speculation that is not backed by either Agno source citation or captured evidence. If an inference exceeds the evidence, label it explicitly as `(inference)` so reviewers can challenge it.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-diagnostic-audit/AUDIT.md && \
      rtk grep -c "^## " .planning/phases/10-diagnostic-audit/AUDIT.md
    </automated>
    <human-check>
      Operator reads AUDIT.md top-to-bottom and confirms: (a) matrix table is at the top, (b) four 5-section blocks present in order, (c) every surface section cites at least one `agno/...:<line>` reference, (d) every absence finding shows the query/command that produced the absence.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `AUDIT.md` opens with a markdown table whose first column is "Surface" and whose first 4 data rows correspond to memory, eval, knowledge, approval in that order.
    - `AUDIT.md` contains exactly 4 H2 sections matching the four surfaces (`grep -c "^## " AUDIT.md` returns at least 4, plus any appendix sections).
    - For each surface section, the literal substring `agno/` appears at least once (linking the Agno source file:line citation).
    - For each surface section, the literal substring `evidence/` appears at least once (linking the raw artifact).
    - The matrix table's "Root-cause tag" column uses stable identifiers (the same identifiers referenced later by phases 11–14).
    - Document satisfies REQUIREMENTS.md DIAG-01: traces every write path from agent run → DB row → AgentOS API response.
  </acceptance_criteria>
  <done>
    AUDIT.md exists, is operator-readable, and every claim is anchored either to an Agno source citation or to an `evidence/` artifact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → VPS docker stack | SSH + Docker exec; only this audit's read-only commands cross this boundary. |
| Audit doc → Git history | Anything committed to `phases/10-diagnostic-audit/` is permanent. Secrets must not cross. |

## STRIDE Threat Register

| Threat ID    | Category          | Component                                  | Disposition | Mitigation Plan |
|--------------|-------------------|--------------------------------------------|-------------|------------------|
| T-10-01      | Information Disclosure | `evidence/wiring.md` env capture          | mitigate    | Redact all values in env capture; only KEY names + `set/unset/****`. Grep for `password\|secret\|token\|key` against value column before commit. |
| T-10-02      | Information Disclosure | Raw curl JSON containing user content     | mitigate    | If `agno_memories` is populated, sample row may include user PII. Truncate sample SELECT to top-level columns only (`SELECT id, user_id, updated_at FROM ...` rather than `SELECT *`), or redact `content` column before committing evidence JSON. |
| T-10-03      | Tampering         | psql session against production DB         | mitigate    | Use a read-only psql user if available; otherwise rely on D-03 discipline (SELECT/`\d` only) and the pg_stat before/after acceptance check in Task 2. |
| T-10-04      | Repudiation       | `$AGENTOS_JWT` echoed in shell history      | mitigate    | Pass JWT via env var, never as a command-line arg that lands in `~/.zsh_history`. Use `read -s` or shell-history-suppression. |
| T-10-SC      | Tampering         | No new package installs in this plan        | accept      | Read-only doc audit; no `pip install` / `npm install` introduced. |
</threat_model>

<verification>
- AUDIT.md present at `.planning/phases/10-diagnostic-audit/AUDIT.md`.
- `evidence/` subdirectory present with at least: `wiring.md`, `databases.json`, `memory.json`, `evals.json`, `knowledge.json`, `approvals.json`, `psql.txt`, `write-gate-grep.txt`, and 4 PNGs.
- `rtk grep -E "password|secret|Bearer eyJ|postgresql://[^*]+:[^*]+@" .planning/phases/10-diagnostic-audit/` returns zero matches (no secrets leaked into committed files).
- pg_stat before/after snapshots in AUDIT.md Appendix A show zero writes for `agno_*` tables.
- Every D-08 section is present for every surface; D-09 matrix table is at the top.
</verification>

<success_criteria>
- DIAG-01 satisfied: a single readable AUDIT.md exists that traces all four write paths end-to-end with code references and row evidence (or absence-of-evidence).
- Each of the four root-cause tags is stable and can be referenced unambiguously by plans 11-XX through 14-XX.
- Operator can identify the failing surface, locate the relevant Agno source line, and re-run the exact psql / curl / UI step that produced the evidence without re-discovering the recipe.
- Plan 10-02 (synthesis + DB-ID-DECISION + BACKLOG) can consume this plan's outputs without ambiguity.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostic-audit/10-01-SUMMARY.md` when done. The summary records: (1) the active DB backend, (2) the captured `db_id`, (3) row counts per surface, (4) the four root-cause tags decided, and (5) any surprises encountered (e.g. an unexpected populated surface, a 503 from /approvals, etc.).
</output>
