---
phase: 10-diagnostic-audit
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - .planning/phases/10-diagnostic-audit/DB-ID-DECISION.md
  - .planning/phases/10-diagnostic-audit/BACKLOG.md
  - .planning/phases/10-diagnostic-audit/AUDIT.md
autonomous: false
requirements: [DIAG-02]
user_setup:
  - Operator must read AUDIT.md and BACKLOG.md and confirm the chosen db_id model (per-agent vs. shared workspace) before this plan is marked complete (D-07 + D-13). Decision is operator-owned; the planner produces evidence and a recommendation.

must_haves:
  truths:
    - DB-ID-DECISION.md states ONE chosen model (per-agent OR shared workspace), not both.
    - DB-ID-DECISION.md contains at least three Agno-source `file:line` citations (D-07).
    - DB-ID-DECISION.md lists explicit consequences for each of phases 11, 12, 13, 14 (D-07).
    - BACKLOG.md is pre-populated with MON-01 (S21808) and MON-02 (S21815) tagged for phase 15 (D-12).
    - BACKLOG.md does NOT promote items into REQUIREMENTS.md (D-13). Operator review happens after.
  artifacts:
    - .planning/phases/10-diagnostic-audit/DB-ID-DECISION.md
    - .planning/phases/10-diagnostic-audit/BACKLOG.md
  key_links:
    - DB-ID-DECISION.md references the AUDIT.md matrix's root-cause tags to show why the chosen model resolves them (or explicitly states the chosen model does NOT resolve a given tag, surfacing the residual work for phases 11-14).
---

<objective>
Synthesize the audit evidence into two operator-facing decision documents: `DB-ID-DECISION.md` (the chosen Agno DB registration model, with source-cited rationale and downstream consequences) and `BACKLOG.md` (parked tech-debt findings, pre-populated with known carryover bugs). Finalize `AUDIT.md` Appendix B to cross-link both.

Purpose: Close out phase 10's two deliverables that depend on AUDIT.md (which plan 10-01 produces). DIAG-02 needs the decision doc; D-11/D-12 need the backlog. The operator can then plan phases 11–15 from a written, defensible foundation.

Output: Two new markdown docs in the phase directory plus a minor AUDIT.md edit to populate Appendix B.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/10-diagnostic-audit/10-CONTEXT.md
@.planning/phases/10-diagnostic-audit/10-RESEARCH.md
@.planning/phases/10-diagnostic-audit/AUDIT.md
@.planning/phases/10-diagnostic-audit/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author DB-ID-DECISION.md with Agno-source-backed rationale</name>
  <files>.planning/phases/10-diagnostic-audit/DB-ID-DECISION.md</files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/AUDIT.md (root-cause tags + per-surface findings — required input)
    - .planning/phases/10-diagnostic-audit/10-RESEARCH.md (headline finding: db_id is a router registry key, not a row partition column)
    - .venv/lib/python3.13/site-packages/agno/db/base.py (lines 56-72: `self.id = id or str(uuid4())` plus default table names — primary citation for the decision)
    - .venv/lib/python3.13/site-packages/agno/os/app.py (lines 1049-1100: `_auto_discover_databases` — builds the `dbs` dict from every agent's `db=` arg — required to explain how the shared vs. per-agent split surfaces in registration)
    - .venv/lib/python3.13/site-packages/agno/os/utils.py (line 246+: `get_db` resolution including the multi-DB-without-db_id 400 error — required to explain why per-agent forces dashboard updates)
    - agentos/app.py (the single shared `db` wiring at lines 25-58 — current state)
  </read_first>
  <action>
    Write `.planning/phases/10-diagnostic-audit/DB-ID-DECISION.md` with the structure mandated by CONTEXT.md D-07. The doc must be persuasive to a future operator who is mid-Phase-12 and wondering whether the eval surface should have its own db.

    Required sections in this order:

    1. **TL;DR** — A single bolded sentence stating the chosen model: either "Pin one shared `BaseDb.id` across all five agents" (Option A) or "Register one PostgresDb per agent with stable user-supplied ids" (Option B). Pick exactly one.

    2. **What `db_id` actually is** — Two-paragraph framing using the three required Agno-source citations (D-07 requires ≥3):
       - `agno/db/base.py:56` — `self.id = id or str(uuid4())`. db_id is a per-DB-instance identifier, auto-generated when not pinned.
       - `agno/os/app.py:1049-1100` — `_auto_discover_databases()` builds `self.dbs: Dict[str, List[BaseDb]]` keyed by that id.
       - `agno/os/utils.py:246+` — `get_db()` looks the request's `db_id` query param up in that dict; with one DB and no id, returns it; with multiple DBs and no id, returns HTTP 400.
       Explicitly debunk the "db_id is a row partition column" misconception so future readers don't re-litigate.

    3. **Options considered**:
       - **Option A — Pin one shared id** (e.g. `PostgresDb(id="ultra-brain-main", db_url=...)`). Pros, cons, migration cost, behavior across redeploys.
       - **Option B — Per-agent DBs with stable ids** (e.g. `PostgresDb(id="agent-chat", db_url=DSN_CHAT)`, etc.). Pros, cons, migration cost (separate DSNs or separate schemas via `db_schema=`), behavior across redeploys.
       Include a comparison table.

    4. **Chosen model + rationale** — Restate the TL;DR. Justify in 3-6 sentences with evidence pointers to AUDIT.md's matrix (which root-cause tags does each option resolve / leave open?). Include an explicit "What this does NOT fix" subsection so the operator sees the residual work.

    5. **Downstream consequences per phase** (D-07):
       - **Phase 11 (Memory)** — Does the decision change anything beyond setting `enable_user_memories=True`? Specifically, must agent factories now take an `id` kwarg for their DB?
       - **Phase 12 (Evals)** — Does the eval harness write to the same DB or a dedicated one?
       - **Phase 13 (Knowledge)** — Does knowledge stay on one shared PgVector instance or follow the per-agent split?
       - **Phase 14 (Approvals)** — Note that approval router uses `os_db` (single) regardless — clarify whether per-agent option requires picking an "AgentOS-level DB" explicitly.
       Each phase gets 2-4 sentences max.

    6. **Migration implications** — If Option A is chosen, can existing rows stay where they are? (Answer: yes — single DB, just pin the id.) If Option B, what happens to existing rows under the previous shared-DB shape? (Answer: stays attributed to whichever new DB the operator decides to point the existing DSN at; cannot be transparently split across new schemas without manual SQL.)

    7. **Citations table** — One markdown table with rows: Citation # | File | Lines | What it proves. Minimum three rows, more if helpful.

    The doc MUST satisfy DIAG-02: an operator reads it and can explain the chosen model with three or more source-backed reasons.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-diagnostic-audit/DB-ID-DECISION.md && \
      rtk grep -c "agno/" .planning/phases/10-diagnostic-audit/DB-ID-DECISION.md
    </automated>
    <human-check>
      Operator reads DB-ID-DECISION.md and confirms: (a) one option chosen (not both), (b) at least 3 Agno source citations present with file:line, (c) each of phases 11-14 has explicit downstream consequence text.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `DB-ID-DECISION.md` exists and contains a TL;DR section whose first sentence picks exactly one of "Option A" or "Option B" (regex match: `\*\*Option [AB]\*\*`).
    - `rtk grep -c "agno/" DB-ID-DECISION.md` returns at least 3 (the three required Agno source citations).
    - Each of the literal strings "Phase 11", "Phase 12", "Phase 13", "Phase 14" appears at least once in the "Downstream consequences" section.
    - The Citations table at the end has at least 3 rows (4 columns: # | File | Lines | What it proves).
    - The document explicitly debunks the row-partition misconception in section 2.
  </acceptance_criteria>
  <done>
    Decision doc exists, justified, cross-referenced to AUDIT.md root-cause tags, and ready for operator approval.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pre-populate BACKLOG.md with carryover bugs + research-surfaced items + audit findings</name>
  <files>.planning/phases/10-diagnostic-audit/BACKLOG.md</files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/10-CONTEXT.md (D-11, D-12, D-13 — backlog rules)
    - .planning/phases/10-diagnostic-audit/10-RESEARCH.md (DIAG-BL-01 through DIAG-BL-04 — candidates surfaced during research)
    - .planning/phases/10-diagnostic-audit/AUDIT.md (any surprises in the surface root-cause hypotheses worth backlogging)
  </read_first>
  <action>
    Write `.planning/phases/10-diagnostic-audit/BACKLOG.md` per CONTEXT.md D-11 / D-12 / D-13. The file is operator-facing and is NOT auto-promoted to REQUIREMENTS.md.

    Required structure:

    1. **Header** with one-sentence purpose + an explicit reminder: "These items are parked. Operator review decides which (if any) get promoted to REQUIREMENTS.md."

    2. **Items table** with columns: ID | Title | Severity (blocking/high/medium/low) | Suggested phase | Repro one-liner.

    3. **Pre-populated rows** (in this order):
       - **MON-01** (medium, phase 15) — Worker.monitor date mismatch causes daily-brief to miss filed items (S21808). Repro: trigger monitor + daily-brief on same day; daily-brief reports zero new items despite monitor having filed N.
       - **MON-02** (high, phase 15) — Vault sync `--delete` flag deletes VPS-generated inbox items (S21815). Repro: run `scripts/sync-vault.sh` while VPS monitor has created new files; observe deletion.
       - **DIAG-BL-01** (high, suggested folded into phase 11 setup) — `agentos/app.py` does not pin `BaseDb.id`. Every redeploy regenerates the UUID; any cached db_id (e.g. in os.agno.com) breaks. Repro: restart agentos container, compare `GET /databases` `id` field before vs. after.
       - **DIAG-BL-02** (medium, phase 14) — If SqliteDb fallback is active, `/approvals` may return HTTP 503 because the backend does not implement approval methods. Repro: unset `POSTGRES_DSN_SESSIONS` locally and probe `/approvals`.
       - **DIAG-BL-03** (medium, phase 12) — No production eval harness; `agno_eval_runs` will stay empty until a CI/cron driver explicitly calls `AccuracyEval(...).run()`. `evals/conftest.py` is pytest-only. Repro: probe `/evals` on a fresh deployment, observe empty array.
       - **DIAG-BL-04** (low, phase 11) — `agentos/agents/*.py` agent factories likely omit `enable_user_memories=True`. Repro: grep `enable_user_memories` across agentos/ — zero matches means memory is structurally disabled.

    4. **Audit-surfaced section** — Append any further items discovered during plan 10-01's execution that don't map to the 4 known surfaces (per D-11). If none, write "No additional items surfaced during audit." Do NOT omit this section; absence-of-evidence is itself evidence.

    5. **Footer** — Restate D-13: "These items are not requirements until the operator promotes them. Editing this file is the only action this phase asks of you after read-through."
  </action>
  <verify>
    <automated>
      test -f .planning/phases/10-diagnostic-audit/BACKLOG.md && \
      rtk grep -c "^| MON-01\|^| MON-02\|^| DIAG-BL-01\|^| DIAG-BL-02\|^| DIAG-BL-03\|^| DIAG-BL-04" .planning/phases/10-diagnostic-audit/BACKLOG.md
    </automated>
    <human-check>
      Operator reads BACKLOG.md and decides which (if any) items to promote to REQUIREMENTS.md before phase 11 planning starts. (Promotion happens in a future session, not in this plan.)
    </human-check>
  </verify>
  <acceptance_criteria>
    - `BACKLOG.md` exists with the items table.
    - All six pre-populated rows present, with the exact IDs MON-01, MON-02, DIAG-BL-01, DIAG-BL-02, DIAG-BL-03, DIAG-BL-04 (regex grep returns 6 matches per the automated verify).
    - Each row has a non-empty repro one-liner.
    - "Audit-surfaced section" header is present even if its body is "No additional items surfaced during audit."
    - Footer restates that BACKLOG items are not auto-promoted to REQUIREMENTS.md.
  </acceptance_criteria>
  <done>
    BACKLOG.md exists with carryover + research + audit findings, ready for operator review.
  </done>
</task>

<task type="auto">
  <name>Task 3: Finalize AUDIT.md Appendix B + commit phase 10 deliverables</name>
  <files>.planning/phases/10-diagnostic-audit/AUDIT.md</files>
  <read_first>
    - .planning/phases/10-diagnostic-audit/AUDIT.md (existing content — must preserve)
    - .planning/phases/10-diagnostic-audit/DB-ID-DECISION.md (just produced)
    - .planning/phases/10-diagnostic-audit/BACKLOG.md (just produced)
  </read_first>
  <action>
    Edit `.planning/phases/10-diagnostic-audit/AUDIT.md` to replace the Appendix B placeholders ("(produced in plan 10-02)") with concrete one-line summaries that link to DB-ID-DECISION.md and BACKLOG.md.

    Required Appendix B content (replacing the placeholder block from plan 10-01 Task 3):

    ```
    ## Appendix B — Companion documents

    - **DB-ID-DECISION.md** — Chosen model: [Option A or B, copy the TL;DR sentence verbatim]. Three Agno-source citations. Downstream consequences per phase 11-14. See `DB-ID-DECISION.md`.
    - **BACKLOG.md** — Six parked items pre-populated (MON-01, MON-02, DIAG-BL-01..04) plus audit-surfaced findings. Operator review owns promotion. See `BACKLOG.md`.
    ```

    Do not modify any other section of AUDIT.md. The matrix table, the per-surface 5-section blocks, and Appendix A (read-only attestation) stay intact.

    After edit, run a final secret-scan grep across the entire phase directory:
    ```
    rtk grep -rE "password=|secret=|Bearer eyJ|postgresql://[^*]+:[^*]+@" .planning/phases/10-diagnostic-audit/
    ```
    Must return zero matches. If matches found: identify the file, redact, re-run.
  </action>
  <verify>
    <automated>
      rtk grep -q "DB-ID-DECISION.md" .planning/phases/10-diagnostic-audit/AUDIT.md && \
      rtk grep -q "BACKLOG.md" .planning/phases/10-diagnostic-audit/AUDIT.md && \
      ! rtk grep -rqE "password=|Bearer eyJ|postgresql://[^*]+:[^*]+@" .planning/phases/10-diagnostic-audit/
    </automated>
    <human-check>
      Operator can navigate AUDIT.md → Appendix B → click through to DB-ID-DECISION.md and BACKLOG.md and the cross-references resolve.
    </human-check>
  </verify>
  <acceptance_criteria>
    - AUDIT.md Appendix B no longer contains the substring "(produced in plan 10-02)".
    - AUDIT.md Appendix B mentions both `DB-ID-DECISION.md` and `BACKLOG.md` by filename.
    - AUDIT.md Appendix B's DB-ID-DECISION bullet quotes the TL;DR sentence from `DB-ID-DECISION.md` verbatim (so the matrix reader doesn't need to context-switch).
    - The final secret-scan grep across `.planning/phases/10-diagnostic-audit/` returns zero matches.
    - All three success criteria from ROADMAP.md Phase 10 are now satisfied: AUDIT.md (DIAG-01), DB-ID-DECISION.md (DIAG-02), and "operator can read both and explain why each surface returns empty data" (the matrix + RC tags make this true).
  </acceptance_criteria>
  <done>
    Appendix B finalized. Phase 10's three deliverables are coherent and cross-linked. Phase 10 closes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit doc → Git history | All three deliverable docs land in git permanently. Same secret-leak risks as plan 10-01. |
| Decision doc → Phases 11-14 planners | Future plan-phase invocations will read DB-ID-DECISION.md as a locked decision. A wrong call here propagates downstream. |

## STRIDE Threat Register

| Threat ID    | Category     | Component                              | Disposition | Mitigation Plan |
|--------------|--------------|----------------------------------------|-------------|------------------|
| T-10-05      | Repudiation  | Decision provenance                    | mitigate    | DB-ID-DECISION.md must list source citations + decision date + AUDIT.md matrix references. No "because I said so" reasoning allowed. |
| T-10-06      | Information Disclosure | Final secret-scan grep                 | mitigate    | Task 3 includes a phase-wide secret-scan grep before close. Zero matches required. |
| T-10-07      | Tampering    | Misclassification of backlog severity  | accept      | Severities are operator-revisable; D-13 explicitly states the operator owns promotion. No automated gate. |
| T-10-SC      | Tampering    | No installs                            | accept      | Read-only doc work. |
</threat_model>

<verification>
- DB-ID-DECISION.md exists with TL;DR, three Agno citations, and per-phase consequences (phases 11-14).
- BACKLOG.md exists with all six pre-populated rows + the audit-surfaced section.
- AUDIT.md Appendix B references both companion docs and quotes the DB-ID-DECISION TL;DR.
- Phase-wide secret-scan returns zero matches.
- ROADMAP Phase 10 success criteria 1, 2, 3 all satisfied.
</verification>

<success_criteria>
- DIAG-02 satisfied: a written DB-ID-DECISION.md states the chosen model with Agno-source-backed rationale.
- D-07 satisfied: chosen model + ≥3 Agno citations + consequences per phase 11-14 + migration implications.
- D-11/D-12 satisfied: BACKLOG.md exists, MON-01 and MON-02 pre-populated, operator owns promotion (D-13).
- All three ROADMAP Phase 10 success criteria are now true. Phase 10 closes.
</success_criteria>

<output>
Create `.planning/phases/10-diagnostic-audit/10-02-SUMMARY.md` when done. The summary records: (1) which option was chosen (A or B) and the one-sentence rationale, (2) which backlog items were audit-surfaced beyond the pre-populated six, (3) confirmation that the phase-wide secret-scan passed, (4) phase 10 status set to ready-for-close.
</output>
