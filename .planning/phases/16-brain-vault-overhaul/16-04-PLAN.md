---
phase: "16-brain-vault-overhaul"
plan: "16-04"
type: "feature"
wave: 4
depends_on: ["16-03"]
files_modified:
  - "ultra_brain/monitor.py"
  - "ultra_brain/brief.py"
  - "ultra_brain/review.py"
  - "agentos/workshop_registry.py"
  - "tests/unit/test_telos_scoring.py"
autonomous: true
must_haves:
  truths:
    - "Daily auto-triage scores new Inbox items with telos_relevance and files high-relevance (≥0.6) to 02-Resources automatically"
    - "Weekly review produces a Telegram-approvable draft listing inbox count, stale projects, and suggested sweeps"
    - "Adding a new repo to workshop_registry auto-creates vault/00-Projects/<repo>/ with _briefing.md, _log.md, _meta.yaml"
    - "Each automation loop is runnable on demand via CLI without requiring a full cron trigger"
  artifacts:
    - path: "tests/unit/test_telos_scoring.py"
      provides: "Unit tests for telos_relevance scoring logic used in daily triage"
      min_lines: 40
  key_links:
    - ultra_brain/monitor.py triage extension calls telos_relevance scoring (reuses logic from scripts/inbox_sweep.py)
    - agentos/workshop_registry.py mirror hook calls vault filing logic from ultra_brain/ingest.py
    - ultra_brain/review.py weekly draft uses Phase 14 HITL approval path for Telegram gating
---

# Plan 16-04 — Automation Loops

<execution_context>
@vault/CLAUDE.md
@vault/_system/operating-manual.md
@ultra_brain/monitor.py
@ultra_brain/brief.py
@ultra_brain/review.py
@agentos/workshop_registry.py
</execution_context>

<tasks>

<task type="tdd">
  <name>Task 1: Extend worker.monitor — daily TELOS-scored auto-triage</name>
  <files>
    ultra_brain/monitor.py
    tests/unit/test_telos_scoring.py
  </files>
  <read_first>
    - ultra_brain/monitor.py (current monitor logic — find where items are written to Inbox/)
    - scripts/inbox_sweep.py (TELOS scoring logic to reuse/extract into a shared helper)
    - vault/_system/telos/dont-do.md (negative priors)
    - ultra_brain/ingest.py (filing helpers)
  </read_first>
  <action>
    RED first: write tests/unit/test_telos_scoring.py.
    Tests:
    1. `test_high_relevance_ai_article` — item titled "Claude Code MCP Integration" with tags
       ["llm", "agents"] scores telos_relevance >= 0.6.
    2. `test_low_relevance_news` — item titled "US Lawmakers Demand Answers on CISA Data Leak"
       with no tech tags scores telos_relevance < 0.3.
    3. `test_low_relevance_esoterica` — item titled "Mastering Dyalog APL" scores < 0.3.
    4. `test_medium_relevance_infra` — item titled "Making deep learning go brrr from first
       principles" scores between 0.3 and 0.7 (off-thesis tech, medium priority).

    GREEN: extract the scoring logic from scripts/inbox_sweep.py into a shared helper
    `ultra_brain/telos_score.py` (or inline into monitor.py if simpler), then extend monitor.py:

    After writing each new item to Inbox/, score it immediately:
    - If telos_relevance >= 0.6: move to 02-Resources/articles/ and update frontmatter.
    - If telos_relevance < 0.3 and item matches a don't-do negative prior: move to
      03-Archives/auto-culled/ (searchable, not deleted). Log at DEBUG.
    - Otherwise (0.3–0.6): leave in Inbox/ with telos_relevance written to frontmatter.
      These appear in the weekly review draft as "needs manual decision."

    Keep change surgical: only touch the filing step after ingestion. Do not restructure monitor.py.
  </action>
  <verify>
    <automated>
      rtk pytest tests/unit/test_telos_scoring.py -q 2>&1 | tail -10
    </automated>
    <human-check>
      Trigger a monitor run on one test URL. Verify: high-relevance item lands in 02-Resources/,
      low-relevance news lands in 03-Archives/auto-culled/, mid-relevance stays in Inbox/ with
      telos_relevance in frontmatter.
    </human-check>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_telos_scoring.py has 4 tests; all pass GREEN.
    - monitor.py scores and files new items on ingestion (not just on sweep).
    - High-relevance (≥0.6) items land in 02-Resources/articles/ immediately.
    - Don't-do negative prior matches (< 0.3) land in 03-Archives/auto-culled/.
    - Mid-range items stay in Inbox/ with telos_relevance written to frontmatter.
  </acceptance_criteria>
  <done>
    4 tests green; monitor files items on ingestion per telos_relevance; Inbox only holds mid-range items.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend review.py — weekly review draft with Telegram HITL gate</name>
  <files>
    ultra_brain/review.py
  </files>
  <read_first>
    - ultra_brain/review.py (current weekly review logic)
    - ultra_brain/brief.py (pattern for Telegram-delivered content)
    - channels/telegram_adapter.py (how HITL approval buttons are constructed)
    - vault/_system/operating-manual.md §Operating cadence
  </read_first>
  <action>
    Extend the weekly review to produce a structured draft and gate the sweep via Telegram HITL.

    Weekly review draft must include:
    1. Inbox item count (remaining mid-range items needing decision).
    2. Projects without activity in >14 days (stale) — list from vault/00-Projects/.
    3. Suggested promotions: inbox items with telos_relevance 0.5–0.59 that are worth reviewing.
    4. Suggested archives: inbox items older than 7 days with telos_relevance < 0.5.
    5. Summary line: "Brain health: {N} inbox · {M} stale projects · {P} promotions ready"

    The draft is sent to Telegram as a message. A HITL inline button pair:
    - "✅ Apply sweep" — triggers the suggested promotions + archives automatically.
    - "🔍 Skip" — defers to next week; no action taken.

    Reuse the Phase 14 HITL approval path (requires_confirmation pattern or equivalent)
    for the Telegram button handling. Do NOT build a new approval mechanism.

    Keep change surgical: extend the existing weekly review function, do not restructure review.py.
  </action>
  <verify>
    <automated>
      python3 -m ultra_brain review --dry-run --vault ~/Documents/second-brain 2>&1 | tail -20
    </automated>
    <human-check>
      Trigger a manual weekly review run. Confirm the Telegram message arrives with the
      brain-health summary and the two inline buttons. Tap "Apply sweep" and verify
      the suggested items are filed/archived.
    </human-check>
  </verify>
  <acceptance_criteria>
    - Weekly review produces the 5-section draft.
    - Draft is delivered via Telegram with two inline HITL buttons.
    - "Apply sweep" applies the suggested filing actions.
    - "Skip" defers without modifying any files.
    - review.py has no structural changes — only the draft content and HITL gate are extended.
  </acceptance_criteria>
  <done>
    Weekly review draft sends to Telegram with HITL buttons; both buttons work correctly.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend workshop_registry.py — project-mirror sync on repo add</name>
  <files>
    agentos/workshop_registry.py
  </files>
  <read_first>
    - agentos/workshop_registry.py (current registry logic — find the add-repo code path)
    - vault/CLAUDE.md §Path And Naming Rules §Recommended project shape (the _briefing/_log/_meta shape)
    - vault/CLAUDE.md §Frontmatter Schemas §briefing §log (templates to use)
    - ultra_brain/ingest.py (vault file writing helpers)
  </read_first>
  <action>
    Extend the registry's add-repo code path to auto-create the vault project mirror.

    After a repo is successfully added to workshop_registry:
    1. Derive the project slug: lowercase kebab-case of the repo name.
    2. Create vault/00-Projects/<slug>/ if it does not exist.
    3. Create vault/00-Projects/<slug>/_briefing.md from the briefing frontmatter template in
       vault/CLAUDE.md — populate: id, title (repo full_name), created_at, project (slug),
       para_tier: 00-Projects, status: draft, privacy: public.
    4. Create vault/00-Projects/<slug>/_log.md from the log frontmatter template — populate:
       id, title, scope: <slug>, append_only: true, privacy: operational.
       Append first log entry: "## [date] add | repo registered in workshop_registry"
    5. Create vault/00-Projects/<slug>/_meta.yaml with: repo_full_name, visibility,
       viewer_permission, registered_at, default_branch.
    6. Log the mirror creation at INFO: "Mirrored repo <slug> to vault/00-Projects/<slug>/"

    Do NOT overwrite existing files if the project mirror already exists (idempotent).
    Do NOT modify any existing workshop_registry behavior beyond the add path.
  </action>
  <verify>
    <automated>
      python3 -c "from agentos.workshop_registry import WorkshopRegistry; print('import ok')"
    </automated>
    <human-check>
      Use the AgentOS /workshop/repos endpoint or the Telegram /register command to add a test
      repo. Verify vault/00-Projects/<slug>/ is created with all three files.
      Run again with the same repo — verify no overwrite (idempotent).
    </human-check>
  </verify>
  <acceptance_criteria>
    - Adding a repo to workshop_registry creates vault/00-Projects/<slug>/ with _briefing.md, _log.md, _meta.yaml.
    - Re-adding an existing repo does not overwrite existing files.
    - workshop_registry.py imports cleanly; existing registry behavior is unchanged.
    - All existing tests in tests/unit/test_workshop_registry.py still pass.
  </acceptance_criteria>
  <done>
    New repo added → vault mirror created; re-add is idempotent; existing tests pass.
  </done>
</task>

</tasks>
