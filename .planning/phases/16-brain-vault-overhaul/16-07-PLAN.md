---
phase: 16-brain-vault-overhaul
plan: 07
type: execute
wave: 5
depends_on: [16-04]
files_modified:
  - ultra_brain/__main__.py
autonomous: true
requirements: [SC6]
gap_closure: true

must_haves:
  truths:
    - "python3 -m ultra_brain review --vault vault --dry-run prints the weekly review draft without sending Telegram"
    - "python3 -m ultra_brain review --vault vault sends the review draft to Telegram with ✅/🔍 HITL buttons"
    - "The 'review' subcommand dispatches to send_weekly_review_telegram(), not write_weekly_review()"
    - "--dry-run and --vault flags are accepted by the review subcommand"
  artifacts:
    - path: "ultra_brain/__main__.py"
      provides: "review subcommand wired to send_weekly_review_telegram() with --dry-run and --vault flags"
      contains: "send_weekly_review_telegram"
  key_links:
    - from: "ultra_brain/__main__.py review handler"
      to: "ultra_brain/review.send_weekly_review_telegram()"
      via: "direct function call when not dry_run"
      pattern: "send_weekly_review_telegram"
    - from: "ultra_brain/__main__.py review handler dry-run path"
      to: "ultra_brain/review.weekly_review_draft()"
      via: "direct function call, prints draft to stdout"
      pattern: "weekly_review_draft"
---

<objective>
Wire the 'review' subcommand in ultra_brain/__main__.py to the new send_weekly_review_telegram() function instead of the old write_weekly_review(). Add --dry-run flag (print draft, no Telegram) and ensure --vault is usable per-subcommand.

Purpose: The new weekly_review_draft() and send_weekly_review_telegram() functions in review.py exist but are unreachable via CLI. The 16-04 plan acceptance criteria required --dry-run and --vault flags on the review subcommand. SC6 requires the weekly review loop to be functional.

Output: __main__.py review handler dispatches to send_weekly_review_telegram(vault) for live runs and weekly_review_draft(vault) for dry-run. Both paths accept --vault from the global root parser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-brain-vault-overhaul/16-04-SUMMARY.md

Current __main__.py review block (lines 149-152):
  if args.command == "review":
      report = write_weekly_review(vault)
      print(f"Weekly review written to {report}")
      return 0

Current import line 16:
  from .review import write_weekly_review

New functions available in ultra_brain/review.py:
  def weekly_review_draft(vault_root: Path) -> tuple[str, str]  # returns (draft_text, sweep_id)
  def send_weekly_review_telegram(vault_root: Path, *, chat_id: str | None = None) -> None

Global --vault argument already on root parser at line 23:
  parser.add_argument("--vault", default="vault", ...)
  vault = Path(args.vault)  # at line 80

The review subparser (line 63) currently has no arguments: sub.add_parser("review")
</context>

<interfaces>
From ultra_brain/review.py:
  def weekly_review_draft(vault_root: Path) -> tuple[str, str]
    Returns: (draft_text: str, sweep_id: str)
    draft_text is a formatted Telegram-style string with inbox count, stale projects, promotions

  def send_weekly_review_telegram(vault_root: Path, *, chat_id: str | None = None) -> None
    Calls weekly_review_draft() internally, sends to Telegram with ✅/🔍 buttons

  def write_weekly_review(vault_root: Path) -> Path  (old function — keep import for now, may be used elsewhere)

From ultra_brain/__main__.py:
  Line 16: from .review import write_weekly_review  — extend to also import new functions
  Line 63: sub.add_parser("review")  — add --dry-run argument here
  Lines 149-152: review handler block — replace body
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update review import, subparser, and handler in __main__.py</name>
  <files>ultra_brain/__main__.py</files>
  <read_first>ultra_brain/__main__.py — read lines 14-18 (imports block), lines 60-65 (review subparser), and lines 148-155 (review handler). Read review.py to confirm exact function signatures before writing.</read_first>
  <action>
Make exactly 3 surgical changes to ultra_brain/__main__.py:

CHANGE 1 — Extend the review import (line 16) to include the new functions:
  Old: from .review import write_weekly_review
  New: from .review import write_weekly_review, weekly_review_draft, send_weekly_review_telegram

CHANGE 2 — Add --dry-run to the review subparser (line 63):
  Old: sub.add_parser("review")
  New:
    review_p = sub.add_parser("review")
    review_p.add_argument("--dry-run", action="store_true", help="Print draft without sending Telegram")

CHANGE 3 — Replace the review handler body (lines 149-152):
  Old:
    if args.command == "review":
        report = write_weekly_review(vault)
        print(f"Weekly review written to {report}")
        return 0
  New:
    if args.command == "review":
        if args.dry_run:
            draft, sweep_id = weekly_review_draft(vault)
            print(draft)
            print(f"\n[DRY RUN] sweep_id={sweep_id} — no Telegram message sent.")
        else:
            send_weekly_review_telegram(vault)
            print("Weekly review sent to Telegram with HITL buttons.")
        return 0

No other changes. write_weekly_review remains imported (it may be used in other contexts).
  </action>
  <verify>
    <automated>python3 -m ultra_brain review --vault vault --dry-run 2>&1 | head -10</automated>
  </verify>
  <done>
    - python3 -m ultra_brain review --vault vault --dry-run exits 0
    - Output contains "Weekly Brain Review" or "Inbox:" (from weekly_review_draft())
    - Output contains "[DRY RUN]"
    - grep -n 'send_weekly_review_telegram' ultra_brain/__main__.py returns at least 2 matches (import + call)
    - grep -n 'weekly_review_draft' ultra_brain/__main__.py returns at least 2 matches (import + call)
    - grep -n 'dry.run' ultra_brain/__main__.py returns at least 2 matches (subparser + handler)
    - python3 -m ultra_brain review --help shows --dry-run flag
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→Telegram | review command sends vault metadata to Telegram API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07-01 | Information Disclosure | Telegram review draft | accept | sends only counts and filenames (stems), no note content; --dry-run available for local-only use |
| T-16-07-02 | Spoofing | HITL button callback sweep_id | accept | sweep_id is a random 12-char hex; no authentication required for local single-user setup |
</threat_model>

<verification>
python3 -m ultra_brain review --vault vault --dry-run
# must exit 0, print draft text, print "[DRY RUN]"

python3 -m ultra_brain review --help | grep dry-run
# must show the flag

grep -n 'send_weekly_review_telegram\|weekly_review_draft' ultra_brain/__main__.py
# must show both in import line AND handler block
</verification>

<success_criteria>
- python3 -m ultra_brain review --vault vault --dry-run exits 0 and prints a draft
- python3 -m ultra_brain review --help lists --dry-run
- grep -c 'send_weekly_review_telegram' ultra_brain/__main__.py returns >= 2
- grep -c 'write_weekly_review' ultra_brain/__main__.py returns >= 1 (import preserved)
</success_criteria>

<output>
Create `.planning/phases/16-brain-vault-overhaul/16-07-SUMMARY.md` when done
</output>
