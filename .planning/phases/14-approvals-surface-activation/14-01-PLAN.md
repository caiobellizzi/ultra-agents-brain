---
phase: 14-approvals-surface-activation
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - agentos/approval_recorder.py
  - tests/test_approval_recorder.py
  - agentos/app.py
autonomous: true
requirements:
  - APPR-01
  - APPR-03
  - OBS-01

must_haves:
  truths:
    - "agentos/approval_recorder.py exists and exports patch_db_for_approval_recording(db)"
    - "Calling patch_db_for_approval_recording on a mock db wraps create_approval, update_approval, and update_approval_run_status"
    - "Each wrapped method emits a JSON OBS log line with path='approval', tool_name, tool_call_id, run_id, approval_id, op, status_from, status_to, latency_ms"
    - "OBS log failures are non-fatal — never raise, never block the underlying DB call"
    - "SqliteDb fallback (POSTGRES_DSN_SESSIONS unset) does not 503 on GET /approvals (DIAG-BL-02)"
    - "patch_db_for_approval_recording is called in agentos/app.py after db is constructed"
    - "All 7 unit tests in tests/test_approval_recorder.py pass"
  artifacts:
    - path: "agentos/approval_recorder.py"
      provides: "DB-method wrapper with OBS-01 logging for all three approval lifecycle hooks"
      contains: "def patch_db_for_approval_recording"
    - path: "tests/test_approval_recorder.py"
      provides: "7 unit tests covering creation log, update log, run_status log, non-fatal failure, idempotency, Sqlite probe"
    - path: "agentos/app.py"
      provides: "Startup wiring — calls patch_db_for_approval_recording(db=db) after db construction"
  key_links:
    - from: "agentos/app.py"
      to: "agentos/approval_recorder.py"
      via: "patch_db_for_approval_recording(db=db) call at startup"
      pattern: "patch_db_for_approval_recording"
    - from: "agentos/approval_recorder.py"
      to: "shared db instance"
      via: "instance-level method replacement on db object"
      pattern: "_approval_recorder_patched"
---

<objective>
Implement approval_recorder.py — a DB-method wrapper that intercepts the three Agno approval lifecycle methods (create_approval, update_approval, update_approval_run_status) on the shared db instance — and wire it into app.py at startup. Covers APPR-01 (approval row creation observable via OBS log), APPR-03 (tool_name/tool_args visible in approval data), and OBS-01 (structured log lines on every approval event).

Purpose: Establishes the observability layer before Telegram bridge work in 14-02. The wrapped methods will capture all approval events regardless of call path (Agno internals, API bridge, run cleanup).

Output: agentos/approval_recorder.py, tests/test_approval_recorder.py (7 tests green), one-line wiring addition in agentos/app.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-approvals-surface-activation/14-CONTEXT.md
@.planning/phases/14-approvals-surface-activation/14-RESEARCH.md
@.planning/phases/14-approvals-surface-activation/14-PATTERNS.md
@.planning/phases/14-approvals-surface-activation/14-VALIDATION.md
</context>

<interfaces>
<!-- Exact patterns the executor needs — extracted from codebase. No codebase exploration required. -->

From agentos/eval_recorder.py (exact wiring pattern to replicate):
- Module-level logger: log = logging.getLogger("agentos.approval")
- patch_classes_for_recording(db: Any) is the entry point; approval recorder uses patch_db_for_approval_recording(db: Any)
- Class-level idempotency guard: if getattr(db, "_approval_recorder_patched", False): return

From agentos/app.py (wiring location, lines 70-71):
- from agentos.eval_recorder import patch_classes_for_recording
- patch_classes_for_recording(db=db)
- Pattern: add approval wiring immediately after eval recorder wiring

From .venv/lib/python3.13/site-packages/agno/db/postgres/postgres.py (DB method signatures):
- db.create_approval(approval_data: dict) -> None
- db.update_approval(approval_id: str, update_data: dict) -> None
- db.update_approval_run_status(run_id: str, run_status: str) -> None
- Both PostgresDb and SqliteDb implement all three methods (DIAG-BL-02 confirmed LOW risk)

From agentos/instrumented_knowledge.py (_emit schema pattern):
- log.info("OBS-01 approval write: %s", json.dumps(record, default=str))
- log.error("OBS-01 approval write failed: %s", json.dumps(record, default=str))

OBS-01 log record schema (D-11):
  path="approval", op="create|resolve|run_status", agent_id, run_id, approval_id,
  tool_call_id, tool_name, status_from, status_to, run_status, resolved_by,
  latency_ms, status="ok|error", [error_type, error_msg on error]
  NOTE: do NOT log raw tool_args; use a truncated/safe summary (max ~120 chars, str repr)
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write failing tests for approval_recorder.py</name>
  <files>tests/test_approval_recorder.py</files>
  <read_first>
    - agentos/eval_recorder.py (test pattern and _emit structure)
    - agentos/instrumented_memory.py (log-and-swallow pattern)
    - tests/test_telegram_adapter.py (mock setup conventions in this project)
    - .venv/lib/python3.13/site-packages/agno/db/postgres/postgres.py (approval method signatures)
  </read_first>
  <behavior>
    Write 7 tests. All MUST fail with ImportError or AttributeError before approval_recorder.py exists:

    1. test_create_approval_logs_ok — mock db with create_approval(approval_data), call patch_db_for_approval_recording(db), invoke wrapped create_approval, assert OBS log emitted with op='create', path='approval', status='ok'
    2. test_obs_log_on_create — same setup, assert log record contains tool_name and tool_call_id extracted from approval_data (use approval_data={'tool_name': 'ingest_to_vault', 'tool_call_id': 'tc-123', 'run_id': 'run-abc', 'agent_id': 'ingest'})
    3. test_tool_name_in_approval_data — call wrapped create_approval, assert the underlying original create_approval was called with the same approval_data (tool_name and tool_args pass-through unchanged — APPR-03)
    4. test_update_approval_logs_resolve — mock db with update_approval, patch, call wrapped update_approval(approval_id='ap-1', update_data={'status': 'approved'}), assert OBS log with op='resolve', status_to='approved'
    5. test_update_run_status_logs — mock db with update_approval_run_status, patch, call with run_id, run_status='completed', assert OBS log with op='run_status', run_status='completed'
    6. test_log_failure_nonfatal — mock db where create_approval raises no exception, but mock the _emit or log.info to raise RuntimeError; assert the wrapped call still returns without exception (OBS failure is non-fatal, D-16)
    7. test_sqlite_fallback_no_503 — create a SqliteDb instance with a temp path (or mock), call patch_db_for_approval_recording on it, assert no AttributeError is raised and _approval_recorder_patched is set (confirms all three methods exist on SqliteDb, satisfying DIAG-BL-02)
    8. test_idempotent_patch — call patch_db_for_approval_recording twice on the same db mock; assert create_approval was only wrapped once (not double-wrapped)

    Run: pytest tests/test_approval_recorder.py -x — all tests must FAIL (ImportError from missing module).
  </behavior>
  <action>
    Create tests/test_approval_recorder.py with 8 unit tests (note: VALIDATION.md lists 7; include the idempotency test as a bonus).

    Import structure at top of file:
    - from __future__ import annotations
    - import json, logging, unittest, time
    - from unittest.mock import MagicMock, patch as mock_patch, call
    - Import pattern: try to import agentos.approval_recorder at class setup (expected to fail until Task 2)

    For test 7 (Sqlite probe): use SqliteDb from agno.db.sqlite.sqlite with a tmp_path fixture or a temp file via tempfile.mktemp(suffix='.db'). The test verifies patch_db_for_approval_recording does not raise and sets the _approval_recorder_patched flag.

    Do not include fenced code blocks. Describe test structure in terms of class/method names and assertions only.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-agents-brain && python -m pytest tests/test_approval_recorder.py -x 2>&1 | grep -E "ERROR|ImportError|ModuleNotFound|FAILED" | head -5</automated>
  </verify>
  <done>tests/test_approval_recorder.py exists with 8 test methods; running pytest on it fails with ImportError or ModuleNotFoundError for agentos.approval_recorder</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement approval_recorder.py and wire into app.py</name>
  <files>agentos/approval_recorder.py, agentos/app.py</files>
  <read_first>
    - tests/test_approval_recorder.py (test expectations, required function signatures)
    - agentos/eval_recorder.py (full file — class structure, _emit pattern, log setup, patch_classes_for_recording entry point)
    - agentos/instrumented_knowledge.py (lines around _emit for JSON schema discipline)
    - agentos/app.py (lines 60-75 — exact insertion point for approval wiring)
    - .venv/lib/python3.13/site-packages/agno/db/sqlite/sqlite.py (confirm create_approval, update_approval, update_approval_run_status exist on SqliteDb)
  </read_first>
  <action>
    Create agentos/approval_recorder.py with:

    Module logger: log = logging.getLogger("agentos.approval") with StreamHandler added only if no handlers exist (matches eval_recorder.py pattern exactly).

    Class ApprovalRecorder with __init__(self, db: Any) and patch(self) -> None:
    - patch() checks _approval_recorder_patched sentinel to be idempotent
    - Wraps db.create_approval, db.update_approval, db.update_approval_run_status via instance-level setattr
    - Each wrapper: records start time, calls original, records latency, calls _emit with appropriate op and fields
    - OBS failures in _emit: catch Exception, log.error the failure, never re-raise (D-16 non-fatal for log writes)
    - _emit(self, *, op, approval_id, tool_name, tool_call_id, run_id, agent_id, status_from, status_to, run_status, resolved_by, latency_ms, status, error_type=None, error_msg=None) writes JSON via log.info or log.error

    For create_approval wrapper: extract tool_name from approval_data.get('tool_name'), tool_call_id from approval_data.get('tool_call_id') or from nested approval_data.get('tool_execution', {}).get('tool_call_id'), run_id from approval_data.get('run_id'), agent_id from approval_data.get('agent_id'). Do NOT log raw tool_args; emit a truncated summary: str(approval_data.get('tool_args', {}))[:120].

    Module-level entry point:
    def patch_db_for_approval_recording(db: Any) -> None:
        ApprovalRecorder(db=db).patch()

    In agentos/app.py: add two lines immediately after the eval_recorder wiring (after line "patch_classes_for_recording(db=db)"):
      from agentos.approval_recorder import patch_db_for_approval_recording
      patch_db_for_approval_recording(db=db)

    The import of approval_recorder must be inside app.py's startup block (not at module top) to mirror the eval_recorder import style already used there.
  </action>
  <verify>
    <automated>cd /Users/caiobellizzi/Documents/Projects/ultra-agents-brain && python -m pytest tests/test_approval_recorder.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_approval_recorder.py all 8 tests pass (exit 0)
    - agentos/approval_recorder.py contains def patch_db_for_approval_recording(
    - agentos/approval_recorder.py contains class ApprovalRecorder(
    - agentos/approval_recorder.py contains _approval_recorder_patched
    - agentos/app.py contains patch_db_for_approval_recording(db=db)
    - python -c "from agentos.approval_recorder import patch_db_for_approval_recording; print('ok')" exits 0
    - pytest tests/ -x passes (no regressions in full suite)
  </acceptance_criteria>
  <done>8 unit tests green, approval_recorder.py exports patch_db_for_approval_recording, app.py calls it at startup, full test suite passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB method wrapper → shared db instance | Patched methods run in-process; failure in OBS emit must not propagate to approval DB operation |
| OBS log output → log handlers | Log output may contain truncated tool_args; must never contain sensitive full args |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | approval_recorder patch | mitigate | Idempotency guard (_approval_recorder_patched) prevents double-wrap from producing double log lines |
| T-14-02 | Denial of Service | OBS emit failure | accept | Log-and-swallow pattern: emit failure never blocks the DB call or approval flow |
| T-14-03 | Information Disclosure | tool_args in OBS log | mitigate | Truncate tool_args to str(args)[:120] in _emit; never log raw args object |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
pytest tests/test_approval_recorder.py -x -v  # 8 tests green
pytest tests/ -x                               # full suite no regressions
grep "patch_db_for_approval_recording" agentos/app.py  # wiring present
grep "_approval_recorder_patched" agentos/approval_recorder.py  # idempotency guard present
</verification>

<success_criteria>
- agentos/approval_recorder.py created with ApprovalRecorder class and patch_db_for_approval_recording entry point
- All 8 tests in tests/test_approval_recorder.py pass
- agentos/app.py calls patch_db_for_approval_recording(db=db) at startup
- Full pytest suite passes (no regressions)
- APPR-01, APPR-03, OBS-01 approval path satisfied by instrumentation layer
</success_criteria>

<output>
Create .planning/phases/14-approvals-surface-activation/14-01-SUMMARY.md when done
</output>
