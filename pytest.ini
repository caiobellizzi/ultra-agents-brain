[pytest]
markers =
    smoke: fast schema-level assertions, no LLM calls (select with -k smoke)
    integration: full agent runs, requires live services (select with -k integration)
    live: integration tests that require a deployed VPS (skip in unit-CI runs)
